# Current Feature

## Status

<!-- Not Started | In Progress | Complete -->

## Goals

<!-- Bullet points of what success looks like -->

## Notes

## History

<!-- Keep this updated. Earliest to latest -->

- **2026-04-16** — Initial Next.js + Tailwind CSS setup bootstrapped from `create-next-app`. Removed default placeholder assets, updated `globals.css` and `page.tsx`, added project context files and CLAUDE.md. Pushed to `https://github.com/michael-rayner92/devstash.git`.
- **2026-04-20** — Dashboard UI Phase 1 completed. ShadCN setup (manual install), `components.json`, `globals.css` updated with OKLCH color variables and Tailwind v4 `@theme inline` mapping, dark mode by default, `/dashboard` route with topbar (search + New item button), sidebar placeholder, and main area placeholder.
- **2026-04-20** — Dashboard UI Phase 2 completed. Collapsible sidebar (desktop toggle via `PanelLeft` button, smooth width transition), item type links to `/items/[type]s`, favorites and recent collections sections, user avatar area with initials/tier badge, mobile-always-drawer via Radix Dialog-based `Sheet` component. `DashboardShell` client component wraps all dashboard routes via `layout.tsx`.
- **2026-04-20** — Dashboard UI Phase 3 completed. Main content area with greeting, 4 stats cards (items, collections, favorites, AI credits), recent collections grid with type-colored gradient cards, pinned items section, and recent items list. `CollectionCard` and `ItemRow` components created. Gradient effect added to collection cards matching reference screenshot.
- **2026-04-27** — Prisma 7 + Neon PostgreSQL setup completed. Schema with all models + NextAuth, initial migration applied, system ItemTypes seeded. `ws` required for Node.js WebSocket support with Neon serverless driver.
- **2026-04-27** — Dashboard collections wired to real DB. Created `src/lib/db/collections.ts` with `getRecentCollections` (dominant type + all types computed per collection) and `getDashboardStats`. Updated `CollectionCard` to accept real DB types and show secondary type icons. Dashboard page is now async, fetches demo user by email, and renders live stats and collections from Neon.
- **2026-04-27** — Demo seed data completed. `prisma/seed.ts` updated with demo user (`demo@devstash.io`), 7 system item types upserted, and 5 collections seeded (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) with 18 items total. `scripts/test-db.ts` updated to verify all seeded data.
- **2026-04-27** — Dashboard items wired to real DB. Created `src/lib/db/items.ts` with `getPinnedItems` and `getRecentItems`. Updated `ItemRow` to accept Prisma `ItemWithType` (drops mock data types, tags rendered from `Tag[]`). Dashboard page fetches all four data sources in parallel; pinned section hidden when empty. All mock data removed from dashboard.
- **2026-04-27** — Stats & sidebar wired to real DB. Created `src/lib/db/sidebar.ts` with `getSidebarItemTypes` and `getSidebarCollections`. Dashboard layout now fetches sidebar data server-side and passes to `DashboardShell` → `SidebarContent`. Sidebar item types driven from DB with Tailwind `capitalize`. Collections section unified with nested Favourites (star icon) and Recent (dominant-type colored circle) sub-groups, plus "View all collections" link. Seed updated with pinned/favorited items and two favorite collections; DB reset and reseeded.
- **2026-05-04** — PRO badge added to sidebar. Created `src/components/ui/badge.tsx` (ShadCN Badge, manual install). Replaced Lock icon on Files and Images item types with a clean, subtle secondary-variant Badge showing "PRO" in uppercase.
- **2026-05-04** — Code quality quick wins from scanner audit. Added `url = env("DATABASE_URL")` to Prisma schema; extracted shared `iconMap`, `relativeTime`, `getInitials` (+ edge case fix) to `src/lib/`; extracted `StatsCard` to `src/components/dashboard/`; dynamic time-based greeting; seed idempotency via delete-then-create; `aria-label` on sidebar button; replaced all `React.X` namespace type references with explicit `import type` statements; added React import convention to coding-standards.md.
- **2026-05-04** — Sidebar item type sort order. Added `ITEM_TYPE_ORDER` constant to `getSidebarItemTypes` in `src/lib/db/sidebar.ts`; types are now sorted snippet → prompt → command → note → file → image → link regardless of DB insertion order. Unknown types sort to the end for future custom type support.
- **2026-05-11** — Auth Setup (Phase 1) completed. Installed `next-auth@beta` and `@auth/prisma-adapter`. Split auth config pattern: `src/auth.config.ts` (edge-safe, GitHub provider), `src/auth.ts` (Prisma adapter + JWT strategy, session callback adds `user.id`). Route handler at `src/app/api/auth/[...nextauth]/route.ts`. `src/proxy.ts` protects `/dashboard/*` via Next.js 16 native proxy support (named `proxy` export). `src/types/next-auth.d.ts` extends Session with `user.id`.
- **2026-05-11** — Auth Credentials (Phase 2) completed. Added `password String?` to User model via Neon MCP migration. Credentials provider placeholder in `src/auth.config.ts` (edge-safe); full bcrypt validation in `src/auth.ts` overrides placeholder. `POST /api/auth/register` validates 8-char minimum, password match, duplicate email, hashes with bcryptjs. Proxy updated to pass `callbackUrl` when redirecting unauthenticated dashboard requests.
- **2026-05-11** — Auth UI (Phase 3) completed. Custom `/sign-in` page (credentials form + GitHub OAuth, open-redirect-safe callbackUrl) and `/register` page (Zod-validated, success toast via Sonner on redirect). Sidebar user area upgraded: real `UserAvatar` component (GitHub image or initials fallback), Radix `DropdownMenu` with Profile link and Sign out. Dashboard layout and page wired to real session via `auth()`. `avatars.githubusercontent.com` added to `next.config.ts` remotePatterns. Dead credentials Account seed block removed.
- **2026-05-18** — Email verification toggle added. `EMAIL_VERIFICATION_ENABLED` env var (default `true`) bypasses the full verification flow when set to `false`. Register route creates users pre-verified and returns `skipVerification: true`; form redirects to `/sign-in?registered=1` with a success toast. `auth.ts` skips the `emailVerified` check. Resend-verification endpoint short-circuits immediately. Set to `false` in `.env.local` to unblock dev testing without a verified Resend domain.
- **2026-05-18** — Forgot password flow completed. "Forgot password?" link added to sign-in page. `/forgot-password` page sends a reset link via Resend; `/reset-password?token=…` page validates the token and updates the hashed password. Uses existing `VerificationToken` model with `identifier = "password-reset:<email>"` (1-hour expiry). 60s per-email cooldown enforced silently — all paths (unknown email, OAuth-only, cooldown) return a uniform 200 per OWASP Forgot Password guidance. No schema migration required.
- **2026-05-18** — Email verification on register completed. `EmailVerificationToken` model added to Prisma schema (migration applied to Neon dev branch). Resend installed; `src/lib/email.ts` sends 24-hour verification link. Register route generates token and fires email; register form redirects to `/check-email?email=…`. `/api/auth/verify-email` validates token, sets `emailVerified`, redirects to `/sign-in?verified=1`. `/api/auth/resend-verification` reissues token with 60s cooldown. `auth.ts` blocks unverified credentials users via typed `CredentialsSignin` subclass (`email_not_verified` code). Sign-in form handles the new error code and token error/success toasts. `scripts/purge-users.ts` added to wipe non-demo users from the DB.
- **2026-05-18** — Profile page completed. `/profile` route (proxy + page-level auth guard). `src/lib/db/profile.ts` fetches user info, total items/collections, and per-type item counts in parallel. `src/actions/profile.ts` has `changePassword` (Zod + bcrypt current-password validation) and `deleteAccount` (redirects to `/sign-in?deleted=1`). `ChangePasswordForm` uses `useActionState` + `useRef`/`form.reset()` to clear inputs on success. `DeleteAccountDialog` uses Radix Dialog for confirmation. Change password section hidden for GitHub-only accounts (`hasPassword` flag). Avatar reads from DB (`profile.image`) for freshness.
- **2026-05-18** — GitHub OAuth redirect fixed. Replaced client-side `signIn("github", { callbackUrl })` with a server action (`src/actions/auth.ts`) that calls `signIn("github", { redirectTo: "/dashboard" })` from `@/auth`. GitHub button in `sign-in-form.tsx` is now a `<form action={signInWithGitHub}>` — eliminates the two-click bug caused by client-side redirect timing.
- **2026-05-18** — Rate limiting for auth completed. `src/lib/rate-limit.ts` utility (Upstash Redis + sliding window, fail-open). Login rate-limited inside `authorize` (5/15 min, IP+email key; falls back to IP for empty-credential spam). Register and forgot-password limited 3/hour by IP. Reset-password limited 5/15 min by IP+token (avoids shared-NAT collateral). Resend-verification limited 3/15 min by IP+email. All routes return 429 with `Retry-After` header and human-readable message. Frontend forms updated to display rate limit errors inline.
- **2026-07-01** — Items list view completed. New `/items/[type]` dynamic route renders a responsive grid (1 col mobile, 2 cols `md`+) of a new `ItemCard` component, left border colored per item type. `getItemTypeByName` and `getItemsByType` added to `src/lib/db/items.ts`, scoped to the signed-in user. Extracted `AuthenticatedShell` from `dashboard/layout.tsx` so `/items` reuses the same sidebar shell; `/items` added to protected routes in `src/proxy.ts`. Empty state redesigned as a dashed-border panel with the type's tinted icon, heading, and subtext instead of a plain text line.
- **2026-07-01** — Vitest unit testing set up. Installed `vitest`; path aliases resolved via Vite 8's native `resolve.tsconfigPaths` (no extra plugin needed). `vitest.config.ts` scopes tests to `src/**/*.test.ts`, node environment. Added `npm run test` / `npm run test:watch`. Example tests colocated next to source: `src/lib/string-utils.test.ts`, `src/lib/relative-time.test.ts` (fake timers), and `src/actions/profile.test.ts` (mocks `@/auth`, `@/lib/prisma`, `bcryptjs`, `next/navigation` to cover `changePassword`/`deleteAccount` without touching the real DB). Updated `CLAUDE.md`, `context/ai-interaction.md` (workflow step 4 now runs tests alongside build), and `context/coding-standards.md` (new Testing section; also fixed a pre-existing unclosed code fence that was swallowing the rest of the doc into a code block).
- **2026-07-01** — Three-column items grid completed. Grid at `src/app/items/[type]/page.tsx:44` now escalates `grid-cols-1 md:grid-cols-2 xl:grid-cols-3` instead of maxing out at 2 columns. `xl` (1280px) chosen over `lg` (1024px) after checking the sidebar shell's expanded width (256px) plus page padding — `lg` left cards too cramped (~240px wide) with the sidebar open. Verified responsively in-browser at 375/800/1100/1300/1440px widths.
- **2026-07-02** — Item detail drawer completed. Right-side slide-in drawer (shadcn `Sheet`) is now the item detail view — no separate item page. Clicking an `ItemCard` (items list) or `ItemRow` (dashboard) opens the drawer and fetches full detail on click via new `GET /api/items/[id]` (auth-scoped to the owner). Added `getItemDetail` + `ItemDetail` type to `src/lib/db/items.ts` (Dates serialized to ISO, collections flattened) with colocated unit tests (`src/lib/db/items.test.ts`). Drawer state managed by client `ItemDrawerProvider` (React context, race-safe fetch via a request-id guard) mounted in `DashboardShell`, so pages stay server components; `ItemCard`/`ItemRow` became clickable client triggers (`role="button"` + Enter/Space + focus ring). Drawer shows type icon + type/language badge, title, description, CONTENT (text/url/file branches), TAGS, COLLECTIONS, and metadata (Type/Updated/ID), plus skeleton + error states; slide-in animation added to `globals.css`. Action bar per screenshot: Copy is functional (clipboard); Favorite (yellow when active), Pin, Edit, Delete are display-only this iteration (mutations deferred). Left the Radix dev-only `aria-describedby` warning as-is (consistent with the existing mobile-nav Sheet). Verified in-browser across command/snippet/link items; lint + build + 21 tests pass.
- **2026-07-06** — Item drawer edit mode completed. Clicking Edit flips the same `Sheet` into inline edit mode (controlled inputs, no form library); the action bar becomes Save/Cancel. Editable for all types: title (required), description, tags (comma-separated → deduped/trimmed array); type-specific: content (snippet/prompt/command/note), language (snippet/command), url (link). Added `updateItem` server action in `src/actions/items.ts` (`{ success, data, error }`, `auth()` + Zod as source of truth — title required, valid URL, tag normalization) and `updateItem` query in `src/lib/db/items.ts` (owner-scoped ownership check, tags fully replaced via `set: []` + `connectOrCreate`, returns the updated `ItemDetail`). Refactored a shared `toItemDetail` mapper + `itemDetailInclude` (DRY across `getItemDetail`/`updateItem`) and ordered tags alphabetically across all item queries so cards and drawer match. On save, the drawer refreshes from the returned detail via `onUpdated` (no second fetch) and calls `router.refresh()` for the card list; Cancel discards; Sonner toasts on success/error. `DrawerBody` gained a view/edit `mode` state keyed by item id. New `src/components/items/item-edit-form.tsx` (label-associated inputs for a11y) and `src/components/ui/textarea.tsx` (shadcn Textarea). Colocated unit tests for the action (6) and query (2). Verified in-browser (command/link edits, save/cancel, empty-title Save guard, tag dedup, invalid-URL error toast); lint + build + 29 tests pass.
- **2026-07-06** — Delete item functionality completed. Clicking Delete in the drawer's action bar now opens a shadcn `AlertDialog` confirmation (new `@radix-ui/react-alert-dialog` dependency; `src/components/ui/alert-dialog.tsx` matches the project's existing `forwardRef` primitive style) instead of doing nothing. Added `deleteItem` server action in `src/actions/items.ts` (`{ success, error }`, `auth()` guard) and `deleteItem` query in `src/lib/db/items.ts` (owner-scoped `findFirst` ownership check before `prisma.item.delete`, returns boolean; `ItemCollection`/`Tag` links clean up via existing cascade — no manual join cleanup needed). New `src/components/items/item-delete-dialog.tsx` renders the Delete trigger + AlertDialog, using `useTransition` + the documented Radix pattern (`e.preventDefault()` in the confirm button's `onClick`) to keep the dialog open until the delete resolves. On success: Sonner success toast, dialog closes, new `onDeleted` callback (mirrors `onUpdated`) resets `ItemDrawerProvider` state to closed and invalidates in-flight fetches, `router.refresh()` drops the card from the list. On error: Sonner error toast, dialog closes, drawer stays open. Colocated unit tests for the action (5) and query (2). Verified in-browser on both the dashboard and `/items/[type]` drawers: confirm deletes and updates stats/collection counts (18→17 items, DevOps 4→3 after cascade), cancel preserves the item and keeps the drawer open, no console errors; lint + build + 36 tests pass.
- **2026-07-06** — Item create modal completed. New "New item" modal (shadcn `Dialog`, new `src/components/ui/dialog.tsx` primitive) opens from the top-bar button and replaces the previously inert button. Type selector (snippet, prompt, command, note, link) reuses the sidebar's non-Pro item types; fields adapt per type (content+language for snippet/command, content for prompt/note, URL for link — required). Added `createItem` server action in `src/actions/items.ts` (`{ success, data, error }`, `auth()` + Zod as source of truth, URL required when type is link) and `createItem` query in `src/lib/db/items.ts` (looks up the system `ItemType` by name, maps it to the right `ContentType`, connect-or-creates tags). Server independently enforces an allow-list of creatable type names rather than trusting the client's Pro filtering. New `src/components/items/item-create-dialog.tsx` (form resets on close/reopen, Create disabled until required fields are filled). Colocated unit tests for the action (8) and query (3). Verified in-browser: created a snippet and a link end-to-end (toast, list refresh, stats update), confirmed empty-title and missing-URL guards, cleaned up test items via delete; lint + build + 47 tests pass.
- **2026-07-09** — Code Editor (Monaco) completed. New `src/components/ui/code-editor.tsx` (Monaco via `@monaco-editor/react` v4.7.0, loaded from the default jsDelivr CDN) replaces the plain `Textarea` for **snippet and command** items across view, edit, and create modes; notes/prompts/other types keep the `Textarea`. Always-dark VS Code styling regardless of the app theme (per the spec), custom `devstash-dark` Monaco theme with a slim translucent scrollbar, macOS traffic-light header carrying a language label (hidden when empty) + Copy button, fluid height via `getContentHeight()`/`onDidContentSizeChange` clamped 120–400px. TS/JS semantic + syntax validation disabled in `beforeMount` so standalone snippets don't show false "cannot find module" squiggles. Extracted shared field-visibility logic to `src/lib/item-fields.ts` (`CONTENT_TYPES`/`LANGUAGE_TYPES`/`isCodeType`, removing the sets previously duplicated in the edit form + create dialog) and language normalization to `src/lib/code-language.ts`; both unit tested (+9 tests). Drawer view mode drops its section-level Copy for code types (the editor header provides it). Known caveat: Monaco logs a benign `Uncaught (in promise) Canceled` rejection from its own dispose path on unmount (drawer/dialog close) — functionality unaffected, left as-is (consistent with the existing Radix `aria-describedby` dev artifact). Verified in-browser across snippet/command view+edit+create, including an end-to-end create that persisted Monaco content; lint + build + 56 tests pass. Committed as `feat: add Monaco code editor for snippets and commands` (`cd551ff`).
- **2026-07-09** — Type-scoped create button added. Each `/items/[type]` page now shows a "New {type}" button that opens the create dialog with that type preselected, via a new optional `initialType` prop on `ItemCreateDialog` (falls back to the first creatable type if the given one isn't creatable, and resets to it on close). Rendered only for creatable (non-Pro) types — file/image pages show no button — gated by fetching `getSidebarItemTypes()` on the page. Styled as an outlined, type-colored button (border + text + icon in the type's color) to differentiate it from the solid neutral top-bar "New item" button directly above it. Verified in-browser (snippet=blue and command=orange preselect correctly; file page has no button); lint + build pass. Committed as `feat: add type-scoped create button to item type pages` (`5cd6e78`).
- **2026-07-13** — Markdown Editor completed. New `src/components/ui/markdown-editor.tsx` (`react-markdown` v10 + `remark-gfm` v4) — a tabbed Write/Preview editor that replaces the plain `Textarea` for **note and prompt** items across create, edit, and view; snippets/commands keep the Monaco `CodeEditor`, and any other content type still falls back to `Textarea`. Always-dark chrome matching the code editor (`#1e1e1e` body / `#2d2d2d` header) with a header Copy button and Write/Preview tabs; readonly mode (drawer view) shows only the Preview tab, edit mode defaults to Write. Write is an auto-growing monospace textarea; both Write and Preview are fluid-height clamped 120–400px, scrolling beyond. GFM support verified end-to-end: headings, bold/italic, inline code, links, ordered/unordered/nested/task lists, blockquotes, fenced code blocks, and tables. Preview styled via a `.markdown-preview` block in `globals.css` (unlayered so it beats Tailwind's preflight reset — layered rules were the initial bug: they lost to preflight until moved unlayered). Extended `src/lib/item-fields.ts` with `MARKDOWN_TYPES` (note, prompt) + `isMarkdownType`, and wired the three integration points (`item-create-dialog.tsx`, `item-edit-form.tsx`, `item-drawer.tsx` — the drawer's section-level Copy is now hidden for markdown too, since the editor carries its own). Colocated unit tests for the new helper (+3, including a disjoint/coverage check that code and markdown types partition all content types). Gotcha found during verification: a running Turbopack dev server served a stale cached `globals.css` (edited before the session started, so no HMR fired) — a `touch` didn't force recompile; clearing `.next` and restarting did. Verified in-browser (note create → view → edit → delete; prompt uses the editor; snippet still uses Monaco with no regression; no app console errors); lint + build + 59 tests pass. Committed as `feat: add markdown editor for notes and prompts` (`a4918ab`).
- **2026-07-13** — File & Image Upload with Cloudflare R2 completed. Uploads file/image items to R2 via the S3-compatible API and serves them back through an auth-scoped proxy. New `src/lib/r2.ts` (lazy, memoized `@aws-sdk/client-s3` client — `uploadToR2`/`getFromR2`/`deleteFromR2` + `publicUrl`/`objectKeyFromUrl`; throws only when used without `R2_*` env, so build stays safe) and `src/lib/file-constraints.ts` (per-kind size/extension rules — images 5 MB, files 10 MB — plus an extension→MIME map). **Validation is by extension + size, not browser MIME**, since dev-doc formats (`.md`/`.yaml`/`.toml`/`.ini`/`.csv`) arrive with empty/mislabeled MIME; the MIME map only drives the served `Content-Type`. `POST /api/upload` (Node runtime, own `auth()` guard — the proxy matcher excludes `/api`) validates → uploads under an owner-scoped key `${userId}/${randomUUID()}${ext}` → `createFileItem`, returning the created `ItemDetail` so the client mirrors the text/url create flow. `GET /api/items/[id]/download` streams bytes via `transformToByteArray` with `X-Content-Type-Options: nosniff` and `Cache-Control: private, max-age=3600` — inline by default (used as the `<img>` src for image previews), `?download=1` forces attachment. In `src/lib/db/items.ts`: added `createFileItem` (contentType `file`) and `getItemFileForDownload`, and **changed `deleteItem` to return `{ fileUrl } | null`** (was boolean) so the delete server action can best-effort remove the R2 object (a failed R2 delete only orphans, never surfaces as a user error). New `src/components/ui/file-upload.tsx` — drag-and-drop drop zone, client-side validation with Sonner errors, image thumbnail via `URL.createObjectURL` (memoized + cleanup-only revoke effect to satisfy the `set-state-in-effect` lint rule), file-info row, and an upload progress bar. Create dialog now surfaces **all 7 system types** (grid 5→4 cols) and uploads file/image via `XMLHttpRequest` for real progress events (fetch/Server Actions can't report upload progress); type pages show the create button for file/image too; the drawer renders an image preview + Download button and hides section Copy for file types. Extended `item-fields.ts` with `FILE_TYPES` + `isFileType`. Added `@aws-sdk/client-s3`; `.env.example` documents `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`/`R2_PUBLIC_URL`. Tests +20 (constraints, file-item queries, delete-returns-fileUrl, action R2 cleanup incl. orphan-tolerance, `isFileType`). Caveat: the 1-hour private download cache means a deleted file's bytes can linger in the owner's own browser cache (unguessable cuid URL, own content — low risk); observed as a cache-hit 200 after delete, while a cache-busted fetch correctly 404s. Verified in-browser end-to-end (image upload → thumbnail preview loaded through the proxy; `.md` upload → correct `text/markdown` round-trip; inline + attachment downloads 200; `.png`-on-file-type rejected client-side; delete removed both DB rows and R2 objects with zero R2 errors in the server log); lint + build + 79 tests pass.
- **2026-07-13** — Image Gallery View completed. New `src/components/items/image-card.tsx` replaces `ItemCard` for **image** items on `/items/images`: a 16:9 (`aspect-video`) thumbnail with `object-cover` fill, loaded as a plain `<img>` through the auth-scoped `GET /api/items/[id]/download` proxy (same pattern as the drawer preview), wrapped in an `overflow-hidden` container so the 5% hover zoom (`group-hover:scale-105`, `duration-300`) stays clipped; footer carries title (truncated), pin/favorite indicators, and relative time. Mirrors `ItemCard`'s interaction contract — opens the item drawer on click/Enter/Space, `role="button"`, focus ring, type-colored hover border via the `--type-color` CSS var. `/items/[type]/page.tsx` branches on `typeName === "image"` inside the existing `grid-cols-1 md:grid-cols-2 xl:grid-cols-3` grid (3-column gallery on desktop per spec); all other types keep `ItemCard`. No new server actions/utilities, so no new tests. Verified in-browser with three uploaded test screenshots (left in the DB as demo content): 3/2/1 columns at 1440/800/375px, portrait image center-cropped by `object-cover`, hover zoom + border, drawer opens with full preview + Download, zero console errors; lint + build + 79 tests pass. Committed as `feat: add image gallery view with thumbnail cards` (`6507742`).
- **2026-07-13** — File List View completed. `/items/files` now renders a single-column list (Google Drive/Dropbox style) instead of the grid: new `src/components/items/file-list-row.tsx` (`FileListRow`) shows an extension-based icon, title + file name, size (`formatSize` from `src/lib/file-constraints.ts`), relative upload time, pin/favorite indicators, and a Download link (`?download=1`) whose `onClick` calls `stopPropagation` so it doesn't also trigger the row's drawer-open handler. New `src/lib/file-icon.ts` exports an `EXTENSION_ICON` lookup map (`.json`→FileJson, `.csv`→FileSpreadsheet, `.yaml`/`.yml`/`.xml`/`.toml`→FileCode, `.ini`→FileCog, else generic File) — looked up inline (`EXTENSION_ICON[extensionOf(fileName)] ?? File`) rather than via a wrapper function, matching the existing `ItemCard`/`ImageCard` icon-lookup pattern (a wrapper function tripped the `react-hooks/static-components` lint rule). `/items/[type]/page.tsx` branches on `typeName === "file"` alongside the existing `image`/grid branches; rows share a bordered container with `last:border-b-0` so only interior rows show a divider. No new server actions/utilities, so no new tests. Verified in-browser: uploaded `.txt`/`.json`/`.csv` test files, confirmed distinct icons per extension, row hover highlight, click-opens-drawer, Download click does not open the drawer, and mobile (375px) stacks file info above the size/date/download row; lint + build + 79 tests pass. Committed as `feat: add file list view for file items` (`f82f3e5`).
- **2026-07-20** — Create Collection completed. New "New collection" button in the top bar opens a modal to create a user-scoped collection with a name (required) and optional description — mirrors the item create flow exactly. New `src/actions/collections.ts` with a `createCollection` server action (`{ success, data, error }`, `auth()` guard, Zod as source of truth: `name` trimmed/required, `description` trimmed→null). New `createCollection` query + serializable `CollectionSummary` type (ISO dates, matching the `ItemDetail` convention) in `src/lib/db/collections.ts`, scoped to the signed-in user. New `src/components/collections/collection-create-dialog.tsx` (shadcn `Dialog`, reuses `FormField`/`Input`/`Textarea`; form resets on close; Create disabled until name is filled; Sonner toasts on success/failure; `router.refresh()` on success). Create uses a **server action** imported into the client dialog (exactly how item create works) — no API route, since items don't use one for create either (API routes here are reserved for GET/download/upload). Top-bar button placed beside "New item" as an `outline`-variant button with a `FolderPlus` icon to differentiate from the solid neutral "New item"; both buttons' labels collapse to icon-only below `sm` (`hidden sm:inline`) so the header + search field aren't crowded on mobile. Colocated unit tests: action (5) and query (2). Verified in-browser (demo user): modal opens, empty-name guard disables Create, creating a collection updated the dashboard grid, sidebar Recent list, and the Collections stat (5→6) via `router.refresh()`, success toast, no console errors; lint + build + 90 tests pass. Committed as `feat: add create collection modal` (`d219f4b`). Note: branched from `feature/copy-icon-on-cards`, so the merge to `main` also carried the previously-unmerged copy-icon commits (`b076c93`, `b435a5a`) along with this feature (merged together per user direction). Left a "Playwright Test Collection" row in the dev DB from verification (no delete-collection UI yet — out of scope).
- **2026-07-20** — Add Item to Collections completed. Items can now be added to zero, one, or many collections from both the **new item** and **edit item** forms. New `src/components/items/collections-field.tsx` (`CollectionsField`) — a toggle-pill multi-select (consistent with `TypeSelector`'s button approach, no new primitive; `aria-pressed`) that fetches the user's collections from a new auth-scoped `GET /api/collections` on mount, with loading/error/empty states; wired into `item-create-dialog.tsx` and `item-edit-form.tsx` (edit pre-fills from `detail.collections`). New `getUserCollections` query (user-scoped, alphabetical, id+name) in `src/lib/db/collections.ts`. Extended `createItem`/`updateItem`/`createFileItem` (query) + `createItem`/`updateItem` (action) to accept `collectionIds`: **create** links via `collections.create`, **update replaces** membership via `collections.deleteMany + create` (mirrors the tag-replacement convention). Security: new `ownedCollectionIds(userId, ids)` helper filters submitted ids to those the user owns via a `{ userId, id: { in } }` query **in the query layer** — foreign/nonexistent ids are silently dropped, so a client can never link into a collection it doesn't own (the action just normalizes/dedupes; the DB layer enforces). Extracted a shared `normalizedStringArray` Zod preprocessor (trim/drop-empty/dedupe) reused by `tags` + `collectionIds`. `collectionIds` also threaded through the `/api/upload` → `createFileItem` path (multipart field) so file/image items support collections too (the create dialog offers all 7 types). Added `orderBy: { collection: { name: "asc" } }` to `itemDetailInclude` so the drawer's COLLECTIONS chips, cards, and the edit selector all match (consistent with the prior alphabetical-tags decision) — applied during `/feature review`. Out of scope (per spec): collection detail pages. Colocated unit tests +4 (create/update/file collection-linking with ownership filtering incl. foreign-id drop; update-clears-when-empty skips the ownership query). Verified in-browser (demo user): create a snippet in DevOps+React Patterns → drawer showed both; edit pre-filled them, swapped DevOps→AI Workflows → drawer + **sidebar counts refreshed** (React Patterns & AI Workflows 3→4) via `router.refresh()`; deleted the test snippet (join rows cascaded); no console errors; lint + build + 94 tests pass. Committed as `feat: add item-to-collection membership to item forms` (`2603b7a`). Branched from `main` (clean single-feature merge).
- **2026-07-20** — Collection Pages completed. The two collection routes that existing links already pointed at now resolve. New `/collections` (`src/app/collections/page.tsx`) — a grid of **all** the user's collections via the existing `CollectionCard`. New `/collections/[id]` (`src/app/collections/[id]/page.tsx`) — a header (name, description, item count) + the collection's items via the existing `ItemCard`; `notFound()` when the collection is missing or not owned. New `src/app/collections/layout.tsx` wraps both in `AuthenticatedShell` (mirrors `items/layout.tsx`); `/collections` added to the `src/proxy.ts` protected-routes check. In `src/lib/db/collections.ts`: extracted a shared `collectionStatsInclude` + `toCollectionWithStats` mapper (DRY with `getRecentCollections`) and added `getCollections(userId)` (all, most-recent-first — kept separate from `getRecentCollections` so the dashboard keeps its DB-level `take: 6`) and `getCollectionWithItems(userId, id)` (owner-scoped `findFirst`; returns collection meta + items in the `ItemWithType` shape the cards consume, ordered by item `updatedAt` desc; `null` for missing/unowned → 404, no ownership leak). Item clicks in a collection open the existing drawer **for free** (the `ItemDrawerProvider` is already mounted in the shell — pages stay server components). All collection `href`s already existed in code (`CollectionCard` → `/collections/${id}`, sidebar Favourites/Recent → `/collections/${id}`, "View all collections" + dashboard "View all" → `/collections`), so no link edits were needed — this feature just makes them resolve (confirmed in-browser). Mixed-type collections render every item with `ItemCard` (per spec — per-type gallery/list is a possible later refinement; the drawer still shows image preview/download). Empty states for both pages (dashed panel, matching `/items`). Colocated unit tests +3 (`getCollections` scoping/mapping; `getCollectionWithItems` null-on-unowned + item flattening). Verified in-browser (demo user, dev on :3001): `/collections` showed all 6 collections with correct counts; React Patterns → detail with 3 `ItemCard`s; item card opened the drawer (showed membership); empty "Playwright Test Collection" showed the empty state; bogus id → 404; sidebar "View all collections" → `/collections`; no console errors; lint + build + 97 tests pass. Committed as `feat: add collection pages` (`b46f70d`). Branched from `main` (clean single-feature merge).
- **2026-07-20** — Collection Edit / Delete / Favorite Actions completed. Collections can now be edited and deleted (Favorite is display-only UI this iteration) from two places: the detail page header and the collection cards. New server actions in `src/actions/collections.ts` — `updateCollection(id, {name, description})` and `deleteCollection(id)` (`{ success, data?, error }`, `auth()` guard, Zod as source of truth, mirroring `createCollection`). New owner-scoped queries in `src/lib/db/collections.ts` — `updateCollection`/`deleteCollection` each do a `findFirst({ where: { id, userId } })` ownership check before mutating (foreign id → null/false → "Collection not found"; no cross-user mutation). **Delete removes only the collection**: the `ItemCollection` join rows cascade away but items are untouched (schema has `onDelete: Cascade` on the join's collection relation, not on `Item`) — verified with a dedicated test and in-browser (deleting a collection dropped the count, items survived). Four new components in `src/components/collections/`: `CollectionEditDialog` (controlled shadcn `Dialog`; a child `EditForm` is mounted only while `open`, so it re-seeds from the collection each open with **no syncing effect**), `CollectionDeleteDialog` (controlled `AlertDialog`, mirrors `DeleteItemDialog`'s `useTransition` + `e.preventDefault()` keep-open pattern; `onDeleted` callback → detail page navigates to `/collections`, otherwise falls back to `router.refresh()` to drop the card), `CollectionCardMenu` (the 3-dots dropdown hosting both dialogs), and `CollectionDetailActions` (Edit / ★ / Delete buttons for the detail header). `CollectionCard` refactored from a server-component `Link` into a client component: whole-card click/Enter/Space navigates via `router.push` (keyboard guarded with `event.target === event.currentTarget` so the menu button doesn't also navigate); used on both `/dashboard` and `/collections`. Detail page header (`src/app/collections/[id]/page.tsx`) gained a `justify-between` row with `CollectionDetailActions`. **Two non-obvious bugs found during in-browser verification and fixed:** (1) clicking a menu item / dialog button navigated the card, because React re-targets events from *portalled* content up the **component** tree (not the DOM tree) — so the portalled menu/dialog clicks reached the card's `onClick`; fixed by stopping `click`/`keydown` propagation at the `CollectionCardMenu` boundary (a wrapper `<div>`). (2) The body was left stuck at `pointer-events: none` after closing a dialog opened from the dropdown — the classic Radix modal-menu→dialog cleanup race; fixed with `modal={false}` on the `DropdownMenu` (verified `document.body` pointer-events returns to `auto` after close). Favorite is intentionally inert in both places (no `onSelect`/`onClick` behavior), consistent with the item drawer's earlier display-only Favorite/Pin. Colocated unit tests +13 (queries: update/delete owner-scoping incl. null/false-on-unowned and delete-leaves-items; actions: auth guard, empty-name rejection, input normalization, not-found, success, throw→generic error). Verified in-browser (demo user): detail-page edit prefilled → saved → header refreshed via `router.refresh()`; detail delete → confirm → redirected to `/collections`, count 6→5, gone from sidebar; card menu opens without navigating, Edit prefills, Delete-confirm cancels cleanly with no pointer-events leak, card-body click navigates; dashboard cards identical; no console errors. Lint + build + 110 tests pass. Committed as `feat: add collection edit, delete, and favorite actions` (`5e2cd26`). Branched from `main` (clean single-feature merge).
- **2026-07-22** — Global Search / Command Palette completed. A `Cmd+K` / `Ctrl+K` command palette with **client-side fuzzy search** across all items and collections. New `src/components/ui/command.tsx` — shadcn `Command` primitive wrapping `cmdk` (installed `cmdk@1.1.1`, React 19 compatible) and reusing the project's `Dialog`; the dialog's default close-X is hidden (`[&>button]:hidden` — palettes close on Esc/outside-click) with an sr-only `DialogTitle`+`DialogDescription` satisfying Radix's a11y contract. New `src/lib/db/search.ts` — `getSearchIndex(userId)` returns a serializable `{ items, collections }` dataset; a **purpose-built lightweight query** (deliberately NOT reusing the heavier `getRecentItems`/`getCollections`): `select`s only the fields the palette needs and counts collection items via `_count`, avoiding loading full item bodies or every collection's items just to size them. Item `preview` mirrors `ItemCard`'s description→content→url precedence, truncated to 120 chars. New auth-scoped `GET /api/search` (mirrors `/api/collections`; the proxy matcher excludes `/api` so the route guards itself). New `src/components/search/command-palette.tsx` — grouped Items/Collections results (empty groups hidden), item rows show the type icon (via shared `iconMap`) + type label, collection rows show a `Folder` icon + item count. **Data-freshness decision:** the palette **re-fetches `/api/search` on every open** (dataset cached in state, silently refreshed) rather than the spec's literal "pre-fetch once on app load" — no per-keystroke round-trips, but always reflects recent create/edit/delete. **cmdk value decision:** each `CommandItem`'s `value` holds only human text (`title + type + preview` for items, `name` for collections) so cmdk's `command-score` fuzzy matching never scores against the cuid id; selection uses an `onSelect` closure, not the value arg. Selecting an item calls `useItemDrawer().openItem(id)` (the `ItemDrawerProvider` is already mounted in `DashboardShell`); selecting a collection `router.push`es to `/collections/[id]`; the palette closes **before** acting so its focus trap tears down before the drawer/nav. Wired into `src/components/dashboard/dashboard-shell.tsx`: a global `keydown` listener toggles the palette on `(metaKey || ctrlKey) && key === "k"` (accepts both Cmd and Ctrl); the previously-inert TopBar search `Input` became a `<button>` trigger with a static `⌘K` `kbd` badge (per the spec's literal "show ⌘K" wording — avoids a client-only `setState`-in-effect and hydration mismatch, while the handler still accepts Ctrl for Windows/Linux). **Follow-up (same session): centered the search bar in the header** — restructured the header into three sections (`flex-1` left wordmark/toggles group, centered search `max-w-md`, `flex-1 justify-end` actions group) so equal-weight sides keep the search dead-center. `min-w-0` is on the **search only**, not the side groups, so on narrow screens the search absorbs the squeeze and the mobile nav toggle is never clipped (first attempt collapsed the hamburger to 0 width; fixed). Verified centering via measured geometry: 0px offset at 1440px (448px wide) and 768px (8px gaps, no overlap with full-text buttons), hamburger+wordmark visible and no horizontal overflow at 375px. Colocated unit tests +2 (`getSearchIndex` user-scoping/mapping; preview precedence + 120-char truncation). Verified in-browser (demo user): palette opens via click **and** Cmd+K; 18 items + 5 collections render grouped with icons/type-labels/counts; fuzzy "debounce"/"devops" filters correctly with empty groups hidden; Enter on an item opened the drawer; clicking the DevOps collection navigated to its page; "No results found." empty state; works from `/collections/[id]` too; zero console errors. Lint + build + 112 tests pass. Committed as `feat: add global search command palette` (`fcd71ca`). Branched from `main` (clean single-feature merge).
- **2026-07-22** — Pagination completed. Numbered page links + prev/next on the three listing pages, fetching only the current page's rows (no fetch-all-then-slice). New `src/lib/pagination.ts` — constants (`ITEMS_PER_PAGE = 21`, `COLLECTIONS_PER_PAGE = 21`, `DASHBOARD_COLLECTIONS_LIMIT = 6`, `DASHBOARD_RECENT_ITEMS_LIMIT = 10`) plus pure helpers `parsePageParam` (missing/non-integer/`<1` → 1; handles `string[]`), `getPageMeta` (clamps the requested page into `[1, totalPages]` so an out-of-range URL resolves to the last page, and returns `skip`/`take`), and `getPageNumbers` (windowed `[1, …, current-1, current, current+1, …, total]` with `"ellipsis"` gap markers). New `src/components/ui/pagination.tsx` — a **server** component (links only): renders the number sequence, an `aria-current` span for the active page, and Prev/Next as greyed-out non-interactive `<span aria-disabled>` at the first/last page (interactive `<Link>` otherwise); returns `null` for a single page; page 1 links omit `?page`. **Per-page decision (spec was internally contradictory** — it defined `COLLECTIONS_PER_PAGE` but only listed `/items/[type]` + `/collections/[id]`, neither of which lists collections; user chose "all three"): `/items/[type]` and `/collections/[id]` paginate items by `ITEMS_PER_PAGE`; `/collections` paginates collections by `COLLECTIONS_PER_PAGE`. DB queries in `src/lib/db/{items,collections}.ts` were reshaped to return `{ items|collections, totalCount, page, totalPages }` via a `count` + `skip`/`take` `findMany` (sequential, not parallel — the clamped `skip` depends on the count first). `getCollectionWithItems` now reads collection meta + `_count.items` via `findFirst`, then loads one page of items through `prisma.itemCollection.findMany` (ownership still enforced by the prior owner-scoped `findFirst`, so no cross-user leak). The three pages read `searchParams.page`, pass it through `parsePageParam`, show the **total** count in the header (not the page slice), and render `<Pagination>` below the grid/list. Dashboard swapped its magic `6`/`10` literals for the named `DASHBOARD_*` constants (from the spec's Notes). Colocated unit tests +18 (`pagination.ts` helpers: parse/clamp/window incl. leading/trailing-ellipsis edges; `getItemsByType` + `getCollections` scoping/skip-take/out-of-range clamp; `getCollectionWithItems` paginated shape + skips the item query when unowned). Verified in-browser (demo user; seeded 25 temp `PGTEST-` snippets → 29 total for 2 pages, then deleted): page 1 = 21 cards with Prev disabled; page 2 = 8 cards with Next disabled and Prev/`1` linking back to `/items/snippets` (no `?page`); `?page=99` clamped to the last page; `/collections` (5) and a collection detail (4 items) correctly hid the controls on a single page; zero console errors. Lint + build + 130 tests pass. Committed as `feat: add pagination to item and collection listings` (`71eafc5`). Branched from `main` (clean single-feature merge).
- **2026-07-23** — Settings Page completed. New protected `/settings` route (`src/app/settings/page.tsx`) hosting the **Change password** and **Delete account / Danger zone** sections, moved off the profile page. `/settings` added to `src/proxy.ts` protected routes + page-level `auth()` guard with `redirect("/sign-in?callbackUrl=/settings")` (mirrors `/profile`); reuses the profile page's standalone layout shell (`max-w-2xl`, back-to-dashboard link, section cards, standalone `<h1>Settings</h1>` since there's no identity card). `ChangePasswordForm` + `DeleteAccountDialog` `git mv`'d from `src/components/profile/` → `src/components/settings/` (single importer, so a clean move for clarity; empty `profile/` component dir removed) — no internal edits, they still import their server actions from `@/actions/profile` (unchanged). New lightweight `getAccountSettings(userId)` query in `src/lib/db/profile.ts` — selects only `password` → `{ hasPassword }` (avoids `getProfileData`'s item/collection counting); `null`→redirect on missing user. Change-password section stays gated behind `hasPassword` (hidden for GitHub-only accounts). Added a **Settings** link (Lucide `Settings` icon) to the sidebar user dropdown in `src/components/dashboard/sidebar-content.tsx`, between Profile and the Sign out separator. Profile page (`src/app/profile/page.tsx`) now renders only identity + usage stats (both moved sections + their imports removed; `getProfileData` still returns `hasPassword` in its contract, now unused by the page — left as-is, cheap and minimal-change). Colocated unit tests +4 (`getAccountSettings`: user-scoping + password-only select, hasPassword true/false, null-on-missing-user). Existing `src/actions/profile.test.ts` unaffected by the component move (mocks the action, not the components). Verified in-browser (demo user): `/settings` renders both sections (change-password form visible → `hasPassword` true); profile page no longer shows them; dropdown Settings link navigates to `/settings`; zero console errors. Lint + build (`/settings` route present) + 134 tests pass. Committed as `feat: add settings page with account actions` (`8dd920b`). Branched from `main` (clean single-feature merge).
- **2026-07-27** — Favorites Page completed. New protected `/favorites` route showing the user's starred items and collections in a compact, terminal-style list (monospace rows, high density, `divide-y` clean lines — no cards). New `src/lib/db/favorites.ts` — `getFavoriteItems`/`getFavoriteCollections`/`getFavorites` (parallel), owner-scoped, `isFavorite: true`, ordered `updatedAt` desc; purpose-built lightweight `select`s (only the fields a row renders, `_count` for collection sizes — deliberately not reusing the heavier `getRecentItems`/`getCollections`, matching the `search.ts` convention). Dates kept as `Date` (not ISO-serialized) since the data crosses only the server-page → client-row boundary, not a JSON API — consistent with how `ItemCard` consumes `ItemWithType`. New `src/app/favorites/{page,layout}.tsx` — page fetches session → `dbUser` → favorites (mirrors items/collections pages incl. the defensive `findUnique`), renders counted **Items (n)** / **Collections (n)** sections, empty state (dashed panel) only when **both** are empty; layout wraps in `AuthenticatedShell`. New `src/components/favorites/favorite-item-row.tsx` (client — opens the existing `ItemDrawer` via `useItemDrawer`, `role="button"` + Enter/Space, mirrors `ItemCard`'s interaction contract) and `favorite-collection-row.tsx` (a plain `Link` to `/collections/[id]` — no client needed). Each row: type icon (colored, via shared `iconMap`), title, badge (item type for items; **item count** for collections, since collections have no type), and `relativeTime` date. `/favorites` added to `src/proxy.ts` protected routes. TopBar **star icon button** (`Button asChild` + `Link`, `aria-label`) added to `dashboard-shell.tsx`. **Also fixed the sidebar Favorites nav link** in `sidebar-content.tsx` — it pointed at the non-existent `/dashboard/favorites`; now `/favorites` with a matching `active` check (the "Recently used" → `/dashboard/recent` link is a separate dangling route, left out of scope). **Review fix:** added `min-w-0` to both row title spans so `truncate` actually works inside the flex row (a flex child won't shrink below its content width without it — long titles would otherwise overflow/push the date off). Sort is by `updatedAt` per the spec's literal wording (reflects any edit, not strictly "favorited at" — there's no `favoritedAt` column). Briefly tried centering the content (`mx-auto`) at the user's request, then reverted per follow-up to keep left-aligned `max-w-screen-lg` consistent with the other listing pages. Out of scope: favorite *toggling* (the drawer's Favorite action stays display-only). Colocated unit tests +3 (`getFavoriteItems`/`getFavoriteCollections` scoping/ordering/select + `_count` flattening; `getFavorites` combines both). Verified in-browser (demo user): 6 favorited items + 2 collections rendered with counts; item click opened the drawer; collection click navigated to its detail page; TopBar star and (fixed) sidebar link both navigate to `/favorites` with the sidebar link showing the active highlight; zero console errors. Lint + build (`/favorites` route present) + 155 tests pass. Committed as `feat: add favorites page` (`d4e102d`). Branched from `main` (clean single-feature merge).
- **2026-07-27** — Editor Preferences Settings completed. New **Editor Preferences** section on `/settings` (auto-saving, no save button) controlling the Monaco `CodeEditor` used for snippets/commands. Added an `editorPreferences Json?` (`JSONB`) column to the `User` model via a Prisma migration (`20260727142310_add_user_editor_preferences`) applied to the Neon **development** branch (verified endpoint `ep-dawn-pine-al63gf92` = `br-plain-smoke-ald1szfh` via MCP before running; never touched production). Had to `prisma generate` explicitly after `migrate dev` — the build's type-check saw a stale client without the new column until the client was regenerated. New `src/lib/editor-preferences.ts` — `EditorPreferences` type, option lists (`FONT_SIZE_OPTIONS` [12,13,14,16,18], `TAB_SIZE_OPTIONS` [2,4,8], `THEME_OPTIONS`), `DEFAULT_EDITOR_PREFERENCES` (13px / 2 / wrap on / minimap off / vs-dark), the Zod `editorPreferencesSchema` (source of truth), and `normalizeEditorPreferences(raw)` which coerces the untyped JSON column into a valid object with **per-field** fallback to defaults (null/partial/tampered JSON can never crash the editor). New `src/lib/db/editor-preferences.ts` — owner-scoped `getEditorPreferences`/`updateEditorPreferences` queries (the update casts through `Prisma.InputJsonValue`, since the fixed-shape interface lacks the index signature Prisma's JSON input wants). New `src/actions/editor-preferences.ts` — `updateEditorPreferences` server action (`{ success, data, error }`, `auth()` guard + Zod, owner-scoped by `session.user.id`). New `src/components/editor-preferences/editor-preferences-provider.tsx` — `EditorPreferencesProvider` (optimistic update → persist → Sonner toast → **rollback on failure**) + `useEditorPreferences()` hook that **falls back to defaults + a no-op updater outside a provider**, so a standalone `CodeEditor` (or a unit test) stays usable. New `src/lib/monaco-themes.ts` defines the three selectable dark themes (`vs-dark`/`monokai`/`github-dark`, all registered in `beforeMount`) plus an `EDITOR_CHROME` map so the editor's macOS-style header/background blend per theme. `src/components/ui/code-editor.tsx` now reads font size, tab size, word wrap, minimap, and theme from context (dropped the hard-coded `devstash-dark`/single-theme constants). New `src/components/settings/editor-preferences-form.tsx` — native `<select>` dropdowns + `role="switch"` toggles, each control's `onChange` auto-saves via the provider. Mounted the provider in `AuthenticatedShell` (normalized from the already-fetched `dbUser` — no extra query) so every `CodeEditor` under the shell reads prefs, and again on the `/settings` page (seeded via `getEditorPreferences`); the two instances don't share state, which is correct since the settings page has no live editor and the app re-fetches on navigation. Colocated unit tests +18 (`normalizeEditorPreferences` null/non-object/valid/partial/per-field-fallback + schema-satisfying output; schema rejects bad font size/theme; action auth-guard/validation/success/throw→generic; query user-scoping/defaults/normalize + write-and-return). Verified in-browser (demo user): section renders defaults → changed theme→Monokai/font→16px/minimap→on, each fired a "saved" toast → reload persisted from DB → opened a snippet drawer and confirmed Monaco actually applied Monokai colors + minimap + 16px + word wrap; zero console errors; reset demo prefs back to defaults afterward. Lint + build + 152 tests pass. Committed as `feat: add editor preferences settings with Monaco integration` (`aea5d78`). Branched from `main` (clean single-feature merge).
- **2026-07-29** — Favorite Toggle completed. The previously display-only Favorite controls are now functional toggles across the item drawer, item cards, collection cards, and the collection detail page (Pin stays display-only per scope). New server actions `toggleItemFavorite(itemId)` / `toggleCollectionFavorite(id)` in `src/actions/{items,collections}.ts` (`{ success, data: { isFavorite }, error }`, `auth()` guard, mirroring the `updateItem`/`updateCollection` shape) backed by owner-scoped **read-then-flip** queries in `src/lib/db/{items,collections}.ts` (`findFirst({ where: { id, userId }, select: { isFavorite } })` → `update` to `!current` → returns `{ isFavorite }` or `null`; never trusts a client-sent boolean, never touches another user's row). No schema change — `isFavorite` already existed on both models. New shared client hook `src/lib/use-favorite-toggle.ts` (`useFavoriteToggle(id, initial, action, onToggled?)`): optimistic update → persist → reconcile with the server value → `router.refresh()`; rolls back + Sonner-toasts on failure. **Cross-surface sync bug caught in-browser and fixed:** each favorite control holds its own `useState`, so toggling in the drawer left the underlying card stale after `router.refresh()`; fixed with React's render-time "adjust state on prop change" pattern (track `prevInitial`, reconcile during render — no effect, no `set-state-in-effect` lint violation), so a toggle on one surface propagates to the others once refresh feeds a new `initial` down. New shared `src/components/items/item-favorite-button.tsx` (always-rendered star: filled amber when favorited, muted + hover/focus-reveal otherwise — matching the cards' copy-button pattern; stops click **and** keydown so it never opens the drawer) wired into `ItemCard`, `ImageCard`, `FileListRow` (replacing the display-only star). Item drawer header star wired via the hook, syncing the drawer's `detail` through a new `onToggled` → `onUpdated({ ...detail, isFavorite })`. `CollectionCard` lifted one hook shared by both its inline star **and** the previously-dead `CollectionCardMenu` "Favorite/Unfavorite" item (new `isFavorite`/`onToggleFavorite` props — the menu item was inert before, now wired). `CollectionDetailActions` gained an `isFavorite` prop and wired its ★ button. All toggles carry `aria-pressed` + "Add/Remove from favorites" labels. `/favorites` rows + dashboard favorites stat update passively via `router.refresh()` (not given their own toggles — out of scope). Colocated unit tests +19 (actions: auth guard, not-found, pass-through ids, success, throw→generic for both; queries: owner-scoping, not-found-skips-update, read-then-flip in both directions for both). Verified in-browser (demo user): item-card toggle (updatedAt bumped, no drawer open), drawer toggle → **card synced on close without reload** (the fixed bug), collection-card inline star + menu Unfavorite both updated the sidebar Favourites list, collection detail ★ updated the sidebar — zero console errors; demo data restored to original favorite set. Lint + build + 171 tests pass. Committed as `feat: add favorite toggle to drawer, collection pages, and cards` (`ce61534`). Branched from `main` (clean single-feature merge).
- **2026-07-29** — Favorites Sorting completed. Client-side sorting on `/favorites` — reorders the already-fetched favorites in the browser, no new query / server round-trip. New pure util `src/lib/sort-favorites.ts` — `sortFavoriteItems` / `sortFavoriteCollections` (non-mutating `[...list].sort`) plus `SORT_KEYS`, `DEFAULT_SORT_KEY` (`date`), `DEFAULT_SORT_DIRECTION` (`desc`); keys **name / date / type**, `asc`/`desc` via a `withDirection` flip, case-insensitive locale compare, and a **deterministic tie-break** (title/name → id, direction-independent) so equal keys never reshuffle between renders. Collections have no item type, so the **`type` key falls back to name** for the Collections section (documented + tested). New client wrapper `src/components/favorites/favorites-list.tsx` — owns the sort state (`useState` key + direction), two `useMemo`d sorted lists, renders a native `<select>` key picker (label-associated, styled like `editor-preferences-form.tsx`) + a direction toggle button (`ArrowUpNarrowWide`/`ArrowDownWideNarrow`, `aria-label` describing current + next state), then the existing Items/Collections `divide-y` sections (JSX moved out of the page). `src/app/favorites/page.tsx` stays a **server** component (auth + `getFavorites` fetch); the non-empty branch now renders `<FavoritesList items collections />`, empty-state panel unchanged (so the sort control only shows when there's something to sort). No persistence / not URL-backed (deliberately client-side, unlike pagination's `?page`); no DB/schema/query changes. Scope: favorites page only. **Review cleanup:** dropped a redundant `cn()` wrapper (single static string) + its now-unused import. Colocated unit tests +10 (`sort-favorites.test.ts`: name/date/type asc+desc for items, type→name fallback for collections, non-mutation, deterministic title/id tie-break). Verified in-browser (demo user, 6 items + 2 collections): Name desc/asc reorder both sections (exact reverse); Type groups prompts→snippets with alphabetical within-group tie-break; direction toggle flips live and updates its own label; collections' Type sorts by name; zero console errors. Lint + build + 181 tests pass. Committed as `feat: add client-side sorting to the favorites page` (`c30e428`). Branched from `main` (clean single-feature merge).
- **2026-07-29** — Pinned Items completed. The previously display-only Pin button in the item drawer is now a functional toggle; pinned items float to the top of the `/items/[type]` listings and populate the dashboard's pinned section (already wired to `getPinnedItems`). New `toggleItemPin(itemId)` server action in `src/actions/items.ts` (`{ success, data: { isPinned }, error }`, `auth()` guard) backed by an owner-scoped **read-then-flip** query in `src/lib/db/items.ts` (`findFirst({ where: { id, userId }, select: { isPinned } })` → `update` to `!current` → returns `{ isPinned }` or `null`; never trusts a client-sent boolean, never touches another user's row) — mirrors `toggleItemFavorite` exactly. No schema change (`isPinned` + `@@index([userId, isPinned])` already existed). `getItemsByType` ordering changed from `{ updatedAt: "desc" }` to `[{ isPinned: "desc" }, { updatedAt: "desc" }]` so pinned floats to the top of the type listings. New client hook `src/lib/use-pin-toggle.ts` (`usePinToggle`) — a deliberate near-parallel of `use-favorite-toggle.ts` (optimistic set → persist → reconcile with the server value → `router.refresh()`; rollback + Sonner toast on failure; same render-time "adjust state on prop change" reconciliation so a drawer toggle propagates to the card after refresh). The drawer's Pin button (`src/components/items/item-drawer.tsx`) wired via the hook with `onClick`, `disabled` while pending, `aria-pressed`, and "Pin item"/"Unpin item" labels, syncing the drawer's `detail` through `onToggled` → `onUpdated({ ...detail, isPinned })`. **Toast decision:** error-only (confirmed with user) — matches the Favorite star beside it; the optimistic fill is the success signal, so a success toast would be noisy/inconsistent. **Scope decisions (confirmed with user):** pinned-first ordering applied to `/items/[type]` only — collection detail (`/collections/[id]`), dashboard "Recent items", and `/favorites` intentionally left as-is (dashboard has its own pinned section; favorites has client sorting). Kept `usePinToggle` as a standalone parallel hook rather than generalizing `useFavoriteToggle` (avoids action-shape ripple / adapter indirection; matches the repo's standalone-hook-per-concern convention). `ItemCard`'s pin icon stays a static indicator (per spec — only the drawer button is interactive). Pin remains items-only; collections are out of scope. Colocated unit tests +8 (query: owner-scoping/not-found-skips-update + read-then-flip both directions; action: auth guard, not-found, pass-through ids, success, throw→generic; plus the `getItemsByType` test now asserts the pinned-first `orderBy`). Verified in-browser (demo user): pinned "Deploy to production" from the drawer → button flipped to "Unpin item" [pressed], card jumped to the **top** of `/items/commands` via `router.refresh()`; unpinned → button reverted; zero console errors. Lint + build + 189 tests pass. Committed as `feat: add pin toggle to the item drawer with pinned-first listings` (`b8baf45`). Branched from `main` (clean single-feature merge).
- **2026-07-29** — Homepage Marketing Mockup completed. A standalone, self-contained marketing homepage prototype in `prototypes/homepage/` (`index.html`, `styles.css`, `script.js`) — **plain HTML/CSS/JS, entirely outside the Next.js app** (not a route, no React/server actions/DB), served for verification via `python3 -m http.server`. **Hero "chaos → order" visual (the main focus):** a three-column `.transform` grid — left **Chaos Container** ("Your knowledge today…") holds 8 source icons (Notion, GitHub, Slack, VS Code, browser Tabs, Terminal, text.txt, Bookmark) injected by JS as inline SVGs and animated with a `requestAnimationFrame` physics loop (velocity drift + wall-bounce + per-node rotation & sine scale-pulse + **pointer-repel** within a 90px radius, speed-clamped, with friction); center **pulsing Transform Arrow** (CSS `@keyframes`); right **Dashboard Preview** ("…with DevStash") — a mini sidebar + 6 item cards with type-colored **top** borders. **Palette follows the spec (not the app's system types):** Snippet `#3b82f6`, Prompt `#f59e0b` (amber, vs app purple), Command `#06b6d4` (cyan, vs app orange), Note `#22c55e`, File `#64748b`, Image `#ec4899`, URL `#6366f1` — all as CSS vars on `:root`. **Sections:** fixed top nav (grows opaque on scroll via an `is-scrolled` class toggled by a passive scroll listener); gradient hero headline + CTAs; 6-card **Features** grid, each card carrying its type accent via a `--c` custom prop + `color-mix()` tints; **AI/Pro** section (Pro badge, capability checklist, a code-editor mockup with hand-tokenized syntax + staggered "AI Generated Tags" CSS animation); **Pricing** (Free vs Pro with "Most Popular" badge and a monthly↔yearly **switch** that rewrites Pro to $6/mo, "/mo, billed yearly", "$72 billed annually. Save 25%.", `aria-checked` synced); final CTA; footer with dynamic `©` year via `new Date().getFullYear()`. **Animations:** chaos rAF loop, CSS arrow pulse, `IntersectionObserver` scroll fade-in (`.reveal` → `.is-visible`), navbar opacity; all gated behind `prefers-reduced-motion` (static placement, no rAF). **Responsive:** `@media` breakpoints collapse the features grid 3→2→1, stack the chaos/arrow/dashboard vertically with the **arrow rotated 90° to point down**, and hide nav links / secondary buttons on small screens. **Bug caught & fixed in-browser:** at 375px the AI editor's long code line forced its grid track to `min-content`, causing 523px horizontal overflow — fixed with `min-width: 0` on `.ai__copy`/`.ai__demo` so the editor's own `overflow-x: auto` contains it (re-verified 375/375, no page overflow). Verified in-browser via Playwright at 1440px and 375px: chaos icons animate (positions change frame-to-frame), pricing toggle math correct, footer year = 2026, navbar `is-scrolled` toggles, mobile stacks with rotated arrow, **zero console errors** (only a benign favicon 404, no favicon shipped). No unit tests (nothing under `src/actions/` or `src/lib/` touched — matches the coding-standards test scope). `npm run build` exit 0 (app unaffected — prototype isn't a route) + `npm run lint` clean. Branched from `main` (clean single-feature merge).
- **2026-07-29** — Homepage (Marketing Landing Page) completed. Turned the static prototype in `prototypes/homepage/` into the real app homepage at `/` (`src/app/page.tsx`), rebuilt on the app stack (App Router + Tailwind v4 + shadcn/ui) — replaces the old `<h1>Devstash</h1>` placeholder. **Public, always-dark page** (no auth guard, not under `AuthenticatedShell`); `page.tsx` is a server component that `await auth()`s and composes 7 sections. New `src/components/home/`: **server** — `hero.tsx` (+ static dashboard-preview), `features.tsx`, `ai-section.tsx` (hand-tokenized code editor + animated AI tags), `pricing.tsx`, `cta.tsx`, `footer.tsx` (server-computed `new Date().getFullYear()`), `brand.tsx`, plus `data.ts` (all repeated blocks data-driven from typed arrays: features, plans, footer columns, mini-cards, dash-nav, AI tags, routes) and `icons.tsx` (custom brand mark + 8 chaos-source SVGs); **client** (only the 4 interactive pieces) — `navbar.tsx` (scroll-opacity via passive listener + session-aware actions), `chaos-animation.tsx` (rAF drift/wall-bounce/rotate-pulse/pointer-repel port of `script.js`), `pricing-toggle.tsx` (owns monthly/yearly `useState`, drives Pro price/period/tagline + `aria-checked`, renders both plan cards since the toggle rewrites the Pro card), `reveal.tsx` (IntersectionObserver fade, rAF-deferred fallback for reduced-motion / no-IO to stay lint-safe). **Shared** `home-button.tsx` wraps the shadcn `Button` (routes through the lightest `ghost` variant, overrides per-tone) so `asChild`+`<Link>` and focus/disabled come for free while keeping the prototype's gradient/ghost/outline look. **Styling:** the prototype's bespoke dark palette (7 accents + surfaces), keyframes (arrow-pulse + mobile 90° rotate variant, tag fade-in), the `.home-reveal` transition, and the `prefers-reduced-motion` guard are scoped under a `.home` wrapper in `globals.css` (uses `isolation: isolate` + a fixed `::before` ambient glow); everything else is Tailwind v4 utilities referencing those vars (e.g. `bg-(--home-bg-card)`, per-item `style={{ "--c": color }}` like `ItemCard`). App OKLCH tokens untouched. Gradients use the v4 canonical `bg-linear-to-*` (not `bg-gradient-to-*`). Geist reused (no prototype Google Fonts link). **Type accent colors kept at the prototype's marketing values** (Snippet #3b82f6, Prompt #f59e0b, Command #06b6d4, Note #22c55e, File #64748b, Image #ec4899, URL #6366f1), defined once in `data.ts` and referenced across the features grid, mini-cards, and AI tags. **Optional auth swap implemented + verified both ways** — signed-in → single "Go to Dashboard" → `/dashboard`; signed-out → "Sign In" (`/sign-in`, hidden below `sm`) + "Get Started" (`/register`). All CTAs resolve: Get Started/Start Pro Trial/final CTA → `/register`; Features → `#features`, Pricing/Unlock Pro → `#pricing`, See how it works → `#features`; footer secondaries stay `#` placeholders (anchor targets offset via `scroll-mt` so content clears the fixed nav). **Review fix:** the decorative chaos→order visual is now `aria-hidden` (matches the prototype, cuts screen-reader noise). **Bug caught by user during review + fixed:** the Free and Pro pricing cards rendered different heights (516 vs 493px) — the `items-stretch` grid stretched each card's `Reveal` wrapper but the inner `<article>` only had content height (the `h-full` was on the wrapper, not the card); added `h-full` to the card `<article>` so it fills the stretched wrapper (re-measured equal 516px, buttons aligned). **Deviations (intentional):** anchor scroll is instant, not smooth — the prototype's `html { scroll-behavior: smooth }` would leak app-wide, so left off; responsive breakpoints mapped to Tailwind's `sm`/`lg` scale (arrow-rotation media query aligned to `lg`/1024px so stacking + rotation flip together) rather than the prototype's 560/760/900px; all 8 chaos icons kept as custom SVGs (incl. GitHub/Slack, which have lucide equivalents) to preserve one consistent hand-drawn set. Out of scope (per spec): no Stripe/checkout, no new server actions / DB queries / API routes, footer secondary links stay placeholders. No unit tests expected (nothing under `src/actions/` or `src/lib/` touched — matches the coding-standards test scope). Verified in-browser (Playwright) at 1440px + 375px: sections reveal on scroll, navbar opacifies, type-colored feature cards, tokenized editor with correct `<T>`/keyword/number colors, pricing toggle math ($8/mo ↔ $6 "/mo, billed yearly" + "$72 billed annually. Save 25%"), chaos animates frame-to-frame, mobile stacks with the arrow rotated down, no horizontal overflow, `prefers-reduced-motion` makes chaos static + reveals visible, zero console errors. Lint + build pass (`/` is now dynamic since it reads the session). Committed as `feat: build marketing homepage from prototype` (`72108dd`). Branched from `main` (clean single-feature merge).
- **2026-08-03** — Responsive Top Bar (Mobile) completed. Decluttered the dashboard header (`src/components/dashboard/dashboard-shell.tsx`) on small screens, where it packed six controls into one row and the search — the only `min-w-0` element — absorbed all the squeeze (usable text area measured at 35px @430px, ~2px @375px, 0 @320px, with 56px of `pr-14` wasted on a `⌘K` badge that isn't even rendered below `sm`). **Below `sm` (640px):** the full fake-input search bar (`hidden … sm:block`) is replaced by a magnifying-glass **icon button** in the right actions group that opens the same command palette; the two ambiguous side-by-side icon-only create buttons (`folder+` / `+`) merge into a **single `+` `DropdownMenu`** offering "New item" and "New collection". **At `sm`+:** unchanged — centered search bar with `⌘K` badge, favorites star, and both labelled "New collection"/"New item" buttons. To let the mobile menu open the dialogs programmatically, added an **optional controlled `open`/`onOpenChange` path** to `ItemCreateDialog` and `CollectionCreateDialog` (`trigger` now optional; existing trigger usage fully backward-compatible via an `isControlled` check that falls back to internal `useState`). The create-dialog open state was **lifted into `DashboardShell`** so the desktop labelled buttons and the mobile menu drive a single dialog instance each (desktop buttons switched from the dialog's own `trigger` to `onClick` setters). Used `modal={false}` on the mobile `DropdownMenu` to avoid the known Radix menu→dialog pointer-events race (verified `document.body` pointer-events returns to `auto` after close — same fix used earlier for the collection card menu). Purely presentational — no server actions, DB queries, or utilities touched, so **no new unit tests** per the coding-standards test scope. Verified in-browser (Playwright, demo user): @375px the header is clean (hamburger · DevStash · search-icon · ★ · +), nothing truncated; the `+` menu opens both dialogs; the search icon opens the palette; @1440px the desktop layout and both labelled buttons are unchanged and still open their dialogs; zero console errors. Lint + build + 189 tests pass. Committed as `feat: make dashboard top bar responsive on small screens` (`f5901cd`). Branched from `main` (clean single-feature merge).
- **2026-08-03** — UI Review Fixes (Homepage + Dashboard) completed. Acted on an in-browser UI/accessibility review of `/` and `/dashboard` at 375 / 768 / 1280px (plus 640 and 1024 boundary checks), signed-out and signed-in as the seeded demo user. **Homepage:** added a mobile nav menu below `md` in `navbar.tsx` (hamburger + Esc-dismissible disclosure panel holding Features / Pricing / Sign In) — the header `Sign In` was `hidden sm:inline-flex` with no hamburger fallback, so at 375px it measured 0×0 and a signed-out phone user had **no sign-in route anywhere on the page**; the header link moved to `hidden md:inline-flex` so it isn't duplicated between `sm` and `md`. Gave `HomeButton` a real focus ring (`ring-2 ring-white/90 ring-offset-2 ring-offset-(--home-bg)`) — shadcn's inherited 1px `ring-ring` (~#767676) was invisible against the primary tone's purple gradient + glow shadow (WCAG 2.4.7 fail); verified by keyboard tab that `:focus-visible` fires and the computed `box-shadow` carries both the offset and the ring. Lightened `--home-text-mute` #6c7280 → **#8b92a3** and added a separate **`--home-accent-text`** (#a5a8f7) for the section eyebrows, since the base `--home-accent` is still needed unlightened for gradients/fills; footer Privacy/Terms moved off the inherited muted color onto `--home-text-dim`. Measured before → after: "No credit card required" 4.08→**6.31**, `/forever` + `/month` 3.68→**5.69**, copyright 3.82→**5.91**, Privacy 3.82→**7.65**, eyebrows 4.4→**8.94**. Fixed the chaos-icon clipping in `chaos-animation.tsx`: the loop clamped both axes against a hard-coded `ICON = 52` with no allowance for the rotate/scale transform, so nodes rendered half-drawn at the `overflow: hidden` edge — now each node's travel range is derived from its **measured** `offsetWidth/offsetHeight` (falling back to `ICON` for the ones hidden below `sm`, which measure 0 and would otherwise collapse the clamp range) and inset by a new `PAD = 8`; the seed grid was reworked to spread across `[PAD, max-PAD]` and the row divisor now derives from the node count. Below `sm` the 8 icons thin to **5** via `hidden sm:grid` on index ≥ 5 (kept as CSS rather than a `matchMedia` render decision to avoid a hydration mismatch; hidden nodes stay in the physics loop but are invisible). Verified over 5 samples across 3.5s: **0px overflow** while positions still change frame-to-frame. Padded footer links (`py-1`) over the 24×24 WCAG 2.5.8 floor — all now ≥24 on both axes (were 93×22 / 46×21 / 38×21). Lifted `--home-file` #64748b → **#94a3b8** so the "Files & Docs" feature card's accent stops reading as a missing border. **Dashboard:** restructured `sidebar-content.tsx` so only the nav lists scroll (`min-h-0 flex-1 overflow-y-auto`) with the upgrade CTA and user area pinned as `shrink-0` siblings — previously the whole column was the scroller with a `flex-1` spacer, which at 375×667 pushed **Upgrade to Pro and Sign out off-screen with no scroll affordance**; also added a close X to the mobile `Sheet` (Esc / outside-click were the only ways out). After: drawer 667px in a 667px viewport, scroller 454px holding 690px of content, user row at y=635–651 — nothing clipped. Held the mobile header treatment until `md` in `dashboard-shell.tsx` (search bar, search icon, both create buttons, and the merged `+` menu all moved `sm:` → `md:`): the search is the header's only `min-w-0` element so it absorbed all the squeeze, measuring a **68px text sliver at 640px** while "New collection" (155px) and "New item" (118px) kept full labels; at 768px the label now fits exactly (193/193, untruncated). Gave `CollectionCard` **real link semantics** — it was a `role="button"` div calling `router.push`, losing cmd-click, middle-click, and the status-bar URL preview; now the title is a `<Link>` stretched over the card by a `before:absolute before:inset-0` overlay, with nested controls raised to `relative z-10` and the card ring driven by `has-[a:focus-visible]:ring-2`. Verified: cmd-click opens a **new tab** while the page stays put, plain click navigates, the actions menu still opens without navigating, and `document.body` pointer-events returns to `auto` after close. Added **`src/lib/type-color.ts`** (`typeTextColor`, `color-mix(in oklab, <color> 72%, white)`) and applied it to the three small-text usages of the stored item-type color (`item-row` badge, `item-card` label, `item-drawer` label) — deliberately **not** changing the seeded `ItemType.color`, which would also lighten every border and gradient that reads fine; snippet 4.22→**5.70**, command 5.39→**7.48**, link 5.87→**8.27**. Added `lg:grid-cols-3` to the collections grid, which disagreed with the stats grid's `lg:grid-cols-4` at 1024px (cards 352px → **229px**). Raised the three section `h2`s from `text-base` to `text-lg` so they outrank the `h3` card titles (both were 16px/600). Enlarged hit areas: collection star 16×16→**28×28** (`p-1.5`), card menu 24×24→**28×28** (was exactly at the floor with no margin), `ItemFavoriteButton` to **26×26** via `p-1.5 -m-1.5` (keeps the visual layout unchanged). Named the dominant collection type in text so colour isn't the only cue — added a `dominantName` field to `SidebarCollection` in `src/lib/db/sidebar.ts` and rendered it as `sr-only` + `title` on the sidebar Recent dots and the collection-card dot. **Findings deliberately not implemented:** (1) *social proof* — real, but testimonials / user counts / logo strips all mean inventing claims about a product with no users; needs real content from the user. (2) *reveal-on-scroll blank page* — only reproducible in full-page captures, where the `IntersectionObserver` never fires; normal scrolling and the reduced-motion fallback both work. (3) The reported *pricing toggle 50×28* target was a **false positive** — it already clears 24×24 on both axes. **Two things worth remembering:** the reviewer's "5total" finding looked like a misread of React's `<!-- -->` text separator and was initially doubted, but `od -c` confirmed the source has the space while the DOM really read `"5total · grouped by dominant type"` — the JSX transform strips the leading space of a text node following an expression, fixed with an explicit `{" "}` (the one other instance of the pattern, `{size} spaces` in `editor-preferences-form.tsx`, renders correctly and was left alone). And the inert "Add item type" `+` button was **removed** rather than padded to 24×24 — enlarging a control that does nothing just makes the dead end easier to hit; custom types are a post-launch Pro feature. A self-inflicted regression was caught in a screenshot mid-implementation: the first pass rendered "· mostly {type}s" inline in the card footer, which overcrowded the row at 4 columns and wrapped "4 items" onto two lines — moved to `sr-only` + `title`. Also fixed `.claude/agents/ui-reviewer.md`, whose `tools:` frontmatter had **escaped underscores** (`mcp\_\_playwright\_\_*`), so the agent silently got no browser and fell back to a static code read; the glob was corrected and `ToolSearch` added (this session's MCP tools are deferred, so a restricted subagent needs it to load them). Agent definitions are cached at session start, so that fix only takes effect next session — the file is left untracked for the user to decide on. Colocated unit tests +3 (`type-color.test.ts`: mix format, notation pass-through, and a guard that the mix direction is toward white). Verified in-browser (Playwright, demo user): zero console errors or warnings on both pages, no horizontal overflow at 375/640/768/1024/1280. Lint + build + **192 tests** pass. Committed as `fix: address UI review findings on homepage and dashboard` (`5cac676`). Branched from `main` (clean single-feature merge).
- **2026-08-03** — Auth Pages Top Nav completed. The homepage marketing nav now renders on `/sign-in` and `/register`, replacing their bare centred-card layout. `Navbar` (`src/components/home/navbar.tsx`) gained an optional `page?: NavbarPage` prop (`"home" | "signIn" | "register"`, **defaulting to `"home"` so the homepage call site is untouched** and the no-regression goal is structurally guaranteed) which drives two things: (1) **section anchors** — `NAV_LINKS` entries now hold a `hash` and a `sectionHref()` helper emits the bare `#features` on `/` but route-carrying `/#features` everywhere else, since the marketing sections only exist on the homepage and the old hard-coded `#features` would have been a dead link; (2) **CTA suppression** — `showSignIn`/`showRegister` drop whichever button would link to the page you are already on (no "Sign In" on `/sign-in`, no "Get Started" on `/register`). The `isAuthenticated ? … : …` ternary became three independent conditionals since the two CTAs now render independently of each other. New `src/components/auth/auth-page-shell.tsx` (`AuthPageShell`, server component) supplies the `.home` wrapper + nav + centred card; `page` is typed `Exclude<NavbarPage, "home">` so the shell can't be misused as the homepage. **The `.home` wrapper is load-bearing, not cosmetic** — every nav colour is a CSS var defined only under `.home` in `globals.css`, so without that ancestor the nav renders unstyled; it also supplies the dark surface (and `.home`'s fixed `::before` ambient glow) so there's no palette seam under the fixed nav. Confirmed with the user before implementing: full marketing dark background (over keeping `bg-background`) and per-page CTA suppression (over showing both buttons everywhere). Both pages became `async` to `await auth()` for the nav's signed-in state, which moved `/sign-in` and `/register` from static (`○`) to dynamic (`ƒ`) in the build output. **Mobile refinement beyond the literal spec:** on the homepage "Sign In" is `hidden md:inline-flex` because two CTAs plus the hamburger crowd a phone header, but on `/register` "Get Started" is suppressed so there's room — hiding the *only* remaining CTA behind a hamburger would be strictly worse. A single `signInInMenuOnly = showSignIn && showRegister` gates both the header's `hidden md:inline-flex` class **and** the mobile menu's duplicate link, so exactly one instance renders in every combination (verified: `/register` @375px shows Sign In inline; `/sign-in`'s menu holds only Features/Pricing with no self-link; homepage menu still holds Sign In). **Bug caught in-browser at short viewports:** the first pass padded `main` with `pt-[66px]` — exactly the nav height — so at 375×450 the register card outgrew the `items-center` centring box, pinned to the padding edge, and left the heading 1px *under* the nav (the page did scroll, so nothing was unreachable, but there was zero clearance). Fixed with symmetric `py-[90px]`: ≥23px clearance below the nav when content overflows, and the card centre lands exactly on viewport centre at 1280×900 (450/450), which also **preserves these pages' original `min-h-screen items-center` viewport centring** rather than shifting it. Also confirmed the sign-in form's `or` divider — whose label uses an opaque `bg-background` (`oklch(0.145 0 0)`) to mask the rule — shows no visible rectangle against `--home-bg` (#0a0b0f); the two darks are near-identical, so no edit to the shared form was needed. Out of scope: the other auth routes (`/forgot-password`, `/reset-password`, `/check-email`) still use the old bare layout. No new unit tests — presentational only, nothing under `src/actions/` or `src/lib/` touched, per the coding-standards test scope. Verified in-browser (Playwright), signed out **and** signed in as the demo user, at 375/768/1280px widths plus 450/560/667/900px heights: nav styling correct with no seam, correct CTA per page, cross-page anchor click lands on `#features` clear of the nav (top at 78px via the existing `scroll-mt`), homepage nav provably unchanged (`#features`/`#pricing` + both CTAs + Sign In still menu-only on mobile), signed-in shows "Go to Dashboard" on both pages, zero console errors, zero horizontal overflow at every size. Lint + build + 192 tests pass. Two unrelated observations: the dev server wedged once on the credentials sign-in POST (no log entry at all; a restart fixed it and the same sign-in then completed in 5.7s) — that path is untouched by this change; and bare `npx tsc --noEmit` reports 65 errors in test files, confirmed identical on a clean stash of `main`, so pre-existing (`npm run build` is the project's gate and passes). Committed as `feat: add marketing top nav to sign-in and register pages` (`08e3a2f`). Branched from `main` (clean single-feature merge).
- **2026-08-05** — Stripe Phase 1 (Core Infrastructure) completed. Scaffolds the billing foundation for DevStash Pro; **nothing is enforced and nothing is user-visible** — the checkout/portal actions exist but no UI calls them. Installed `stripe@22.4.0` (pins API `2026-07-29.dahlia` itself); **no `apiVersion` passed** — `StripeConfig.apiVersion` is typed to the exact `LatestApiVersion` literal, so any other string is a type error (verified against the installed `.d.ts`, plus Context7 for the lazy-client pattern). Migration `20260805114329_add_stripe_subscription_fields` added `stripeSubscriptionStatus` / `stripePriceId` / `stripeCurrentPeriodEnd` to `User`, applied to the Neon **development** branch only (endpoint `ep-dawn-pine-al63gf92` confirmed = `br-plain-smoke-ald1szfh` via MCP *before* running), then `prisma generate` run explicitly per the known stale-client gotcha. New `src/lib/usage-limits.ts` — deliberately **pure** (zero imports, client-safe): `FREE_ITEM_LIMIT = 50`, `FREE_COLLECTION_LIMIT = 3`, `getPlanLimits`, `itemLimitError`, `collectionLimitError`, `uploadNotAllowedError`, and `billingEnforced()` as a **function** not a const, so `vi.stubEnv` works without `resetModules` + dynamic import; `BILLING_ENFORCED` defaults **off** (inverse of `EMAIL_VERIFICATION_ENABLED`). New `src/lib/stripe.ts` — lazy memoized `getStripe()` mirroring `lib/r2.ts` so import never throws without env (keeps `build` safe), plus `PRICE_IDS`, `baseUrl()`, and `isProStatus()` granting **only `active` + `trialing`** (the `past_due`-revokes-immediately decision lives here). New `src/lib/db/billing.ts` — `getBillingStatus` / `getPlanUsage` / `setStripeCustomerId` / `findUserByStripeCustomerId` / `syncSubscription`, all `userId`-first except the webhook's reverse customer lookup. New `src/actions/billing.ts` — `createCheckoutSession(interval)` / `createBillingPortalSession()` in the `{ success, data, error }` shape; they **return the Stripe URL rather than calling `redirect()`** (which throws and would be swallowed by the try/catch convention), set `client_reference_id` + `metadata.userId` + `subscription_data.metadata.userId` for Phase 2's webhook, and pass `idempotencyKey: "customer:${userId}"` on customer creation — which also makes the create-succeeded-but-DB-write-failed case self-heal, since a retry gets the same customer back (within Stripe's 24h key window). **Session plumbing:** `isPro` added to the `jwt` callback's *existing* user query and exposed as `session.user.isPro`, re-read on every validation so a Phase 2 webhook flip shows after a reload; the `passwordChangedAt` sign-out-everywhere check was preserved and still skipped on the sign-in pass (`user` present, no prior `iat`). **Two non-obvious things found:** (1) the JWT type augmentation must target **`@auth/core/jwt`**, not `next-auth/jwt` as the Auth.js v5 docs and the spec both say — `next-auth/jwt` only `export *`s the interface, and declaration merging has to hit the declaring module, so augmenting it silently does nothing and leaves `token.isPro` as `unknown`, surfacing as `Type '{}' is not assignable to type 'boolean'` (`unknown ?? false` narrows to `{}`) and failing the build; (2) the claim (from the plan) that always-syncing `isPro` costs **zero** extra queries is only true on the re-validation path — the **sign-in pass** previously returned early via `if (user) return token` and did no query at all, so it now costs one, which is the price of `isPro` being correct on the first request after sign-in rather than one validation later (comment corrected during `/feature review` to say so). **Verified in-browser** rather than assumed: flipping `isPro` directly in the dev DB made `/api/auth/session` return `isPro: true` **on the next validation with no re-sign-in**, and the sidebar badge then flipped `FREE` → `PRO` with the upgrade CTA disappearing; separately, changing the demo password nulled the session and bounced `/dashboard` → `/sign-in`, and signing in with the new password immediately succeeded (which only works if the `!user` guard skips the check on the sign-in pass) — demo password, `isPro`, `passwordChangedAt` and all Stripe columns restored to seed state afterwards. A `JWTSessionError` seen on the first page load was a **stale `authjs.session-token` cookie**, not a regression — it vanished after `context.clearCookies()` and never recurred. **Stripe account:** product `Devstash Pro Plan` (`prod_V12tZGzbuydXaC`) already had both prices active at the right amounts but in **AUD, not USD** as the plan assumed (confirmed intentional; plan §5 corrected — currency can't be changed on an existing price). **No customer portal configuration existed at all** (`configurations.list` → 0), which would have made `createBillingPortalSession` fail at runtime *and* dead-ended Phase 2's lifecycle tests; created via the API as `bpc_1U13f3GhY8WlH9zlrkUejph5` (default, active) with cancel `at_period_end`, payment-method update, invoice history, customer update, and switch-plans with both prices — gotcha: `features.subscription_update.products` is **not returned unless you `expand` it**, so a bare read looks misconfigured when it isn't (cost one redundant update call to discover). **Open questions resolved by user decision:** no trial (checkout charges immediately → the homepage "Start Pro Trial" copy fix is now a Phase 2 deliverable) and `past_due` revokes Pro immediately. Proration on plan switches was set to Stripe's portal default `create_prorations` as an implementation default and resurfaced as a Phase 2 open question. **Phase 2 spec updated** in the same session so nothing carries silently: added a Naming Note (the plan's §4.3–4.5 all `import … from "@/lib/billing"`, which doesn't resolve — it's `@/lib/usage-limits` / `@/lib/stripe`, with `src/lib/db/billing.ts` as a confusing near-miss), a Decisions-Carried-From-Phase-1 section, the copy fix as numbered deliverable #7, a fix to an Out-of-Scope line that was excluding that copy fix by reference, and an Open Questions section covering proration, the unused `STRIPE_PUBLISHABLE_KEY`, `getPlanUsage`'s two count queries per create, and whether the dashboard should warn as a Free user nears a limit. **Review findings:** the overstated comment was fixed; two were logged rather than fixed — both billing actions call `getBillingStatus` and discard its two `count` queries (rare clicks, but reads misleadingly), and a `past_due` user reads as Free so clicking Upgrade would create a **second** Stripe subscription (unreachable in Phase 1; Phase 2's `BillingSection` should route such users to the portal instead — **not yet captured in the Phase 2 spec**). Also noted: `src/lib/mock-data.ts` is dead (nothing imports it), and Phase 2's "grep confirms `isPro` is written only by the webhook" precondition already holds. Tests +53 (limit boundaries 49→allowed/50→blocked, query scoping and `select` shapes, actions with Stripe mocked at the module boundary via `vi.hoisted` for assertable spies and a mutable `PRICE_IDS`); added `isPro: false` to the four existing `Session` fixtures since the field is now required. Lint + build + **245 tests** pass. Committed as `feat: add Stripe billing core infrastructure` (`0230a25`). Branched from `main`; the commit also carries the previously-untracked Stripe spec/research/plan docs.
- **2026-08-05** — Stripe Phase 2 (Integration & UI) completed. Turns Phase 1's scaffolding into a working subscription flow: the webhook that flips `isPro`, enforcement at all three create paths, and the billing UI on `/settings`. New `src/app/api/stripe/webhook/route.ts` (`runtime = "nodejs"`) handling `checkout.session.completed` + `customer.subscription.{created,updated,deleted}`; **signature verification is the only auth** (never calls `auth()`; the proxy matcher already excludes `/api`), raw body via `await req.text()` + `constructEventAsync`, 500 on handler failure so Stripe retries, 200 for unhandled types, and unknown customers resolved to a **no-op 200 rather than an error**. New `src/components/settings/billing-section.tsx` (usage meters, monthly/yearly segmented toggle, Upgrade for Free; renewal date + Manage subscription for Pro) and `src/components/settings/checkout-toast.tsx`. Gates added to `createItem`, `createCollection`, and — the easy-to-miss third path — `POST /api/upload`, where file/image items are created via `createFileItem` and bypass the `createItem` action entirely. Sidebar upgrade block became a real `<Link href="/settings#billing">`; dashboard stats read `getPlanLimits(dbUser.isPro)` instead of hard-coded Free copy; homepage `PRO_PLAN.cta.label` fixed `"Start Pro Trial"` → `"Upgrade to Pro"` (no trial exists, checkout charges immediately — a refund/chargeback risk). **Verified against live Stripe test mode**, not assumed: installed the Stripe CLI via Homebrew (v1.45.1) and — since `stripe login` is interactive and unavailable in this environment — used `stripe listen --api-key "$STRIPE_SECRET_KEY"`, which works without pairing. Real checkout with `4242…` showed **A$8.00/month**, all events 200, DB got `isPro=true` + customer/subscription ids + status `active` + **`stripeCurrentPeriodEnd` non-null**; the sidebar badge flipped `FREE` → `PRO` **with no re-sign-in** (Phase 1's JWT sync confirmed). Lifecycle: monthly→yearly moved the period to 2027-08-05 and updated `stripePriceId`; `cancel_at_period_end` kept Pro at `active`; `past_due` revoked instantly; real `deleted` cleared subscription/price/period while keeping the customer id; replaying an event id produced **identical DB state**. Security: unsigned → 400, garbage signature → 400, valid-signature-over-a-different-body → 400, unknown customer → **200 with no write**, unhandled type → 200; grep confirms `isPro` is written only by `syncSubscription`, whose sole callers are the webhook. Gating at `BILLING_ENFORCED=true`: blocked at exactly 3/3 collections and 50/50 items with the right toasts and **no rows written**, Free upload → 403 with **zero R2 activity in the log**; Pro passed all three; back at `false` a Free user passed all three. **Five findings.** (1) The `current_period_end` gotcha is real (confirmed item-level at `SubscriptionItems.d.ts:54`), but the bigger discovery is that the **Stripe account's default API version is `2020-08-27`** while stripe-node v22.4.0 pins `2026-07-29.dahlia` — webhook payloads are typed as dahlia but shaped as 2020-08-27 at runtime. Benign today only because that old rendering carries `current_period_end` at **both** sub and item level; **set the endpoint's API version explicitly when creating the production endpoint** (saved to memory). (2) **A toast fired on mount is silently dropped** — Sonner's `<Toaster/>` sits *after* `{children}` in the root layout, so it subscribes *after* page mount effects run. Cost real debugging time: the effect provably ran (it stripped the URL param) yet nothing rendered, and a control test proved Sonner worked on the same page. Fixed by firing from `setTimeout(…, 0)`. `/sign-in`'s toasts are unaffected — `useSearchParams` defers that component past hydration (verified, so no pre-existing bug to fix). (3) A `useRef` "already handled" guard **actively causes** that failure, since StrictMode's discarded first pass consumes the only call; stripping the query param is what provides idempotence. (4) **Proration reproduced concretely** (Open Question 1): monthly→yearly through the portal charges **A$136.00 today** (−A$8.00 credit, +A$72.00 proration, +A$72.00 new cycle). User decision: **log it, decide later** — `bpc_1U13f3GhY8WlH9zlrkUejph5` left on `create_prorations`. (5) Stripe **adaptive pricing** showed EUR (€5.08) by default from the test browser's geo, with an AUD selector. **Three deliberate deviations.** Added `userExists` to `src/lib/db/billing.ts`: the plan's `resolveUserId` prefers Stripe metadata over the customer lookup, so a `userId` written by another environment would reach `prisma.user.update`, throw, 500, and put Stripe in a retry loop — defeating the spec's own unknown-customer rule; now the stored customer id wins (our DB is the source of truth) and metadata is only a DB-verified fallback. Placed the upload gates **after** `req.formData()` rather than immediately after the auth guard (§4.5) so the request body is consumed before responding (an early response mid-upload risks the client seeing a connection reset instead of the 403), and before `validateUpload` so a Free user gets the Pro message rather than an extension nitpick. Labelled amounts `$8 AUD` / `$72 AUD` since Checkout charges AUD. **`BillingSection` also closes Phase 1's logged review finding**: a `past_due` user reads as Free, so an Upgrade button would create a **second** subscription — lapsed subscriptions now route to the portal ("Update payment details") instead, verified in-browser. **Review caught one thing worth recording**: the item-limit gate in `/api/upload` is **provably unreachable** — `uploadNotAllowedError` returns null only when `getPlanLimits` yields `UNLIMITED`, which sets `items: null`, so `itemLimitError` can never fire there. The spec mandated both gates so it was kept, but commented as unreachable rather than left reading as live cover for the 50-item cap (it activates only if Free ever gets uploads). Also noted, not fixed: Stripe does not guarantee event **ordering**, so a stale `updated` landing after `deleted` would resurrect the subscription — the plan's "absolute state" reasoning covers redelivery, not reordering; carried to Phase 3. Tests +12 (limit gates blocked/allowed/Pro/unenforced/account-not-found for both actions, `userExists` scoping); `getPlanUsage` mocked in both action test files with an under-limit default so existing cases still exercise validation. Test data cleaned up (34 temp rows removed with the user's OK; the two uploaded files deleted through the app so their R2 objects went too) and the dev DB returned to 18 items / 5 collections. **Still-open questions carried forward:** `STRIPE_PUBLISHABLE_KEY` remains unused; `getPlanUsage` adds two `count` queries per create; and whether the dashboard should warn as a Free user nears a limit. Lint + build + **257 tests** pass. Committed as `feat: wire Stripe subscriptions end to end` (`d620fb7`). Branched from `main` (clean single-feature merge).
- **2026-08-05** — Gate `/items/files` and `/items/images` behind Pro completed. Free users hitting either route directly now see an upgrade panel instead of the listing; Pro users see the normal page, unchanged. New `src/components/items/upgrade-required.tsx` (`UpgradeRequired`) — dashed-border panel matching the existing empty-state style, `Lock` icon, "{type}s are a Pro feature" heading, and an "Upgrade to Pro" button linking to `/settings#billing` (same destination as the sidebar's existing CTA). `src/app/items/[type]/page.tsx` computes `requiresUpgrade = isFileType(typeName) && !getPlanLimits(dbUser?.isPro ?? false).uploads` — reusing the same `isFileType` (`src/lib/item-fields.ts`) and `getPlanLimits(...).uploads` (`src/lib/usage-limits.ts`) signals already used to gate the upload API, so this respects the existing `BILLING_ENFORCED` kill-switch: with enforcement off (the dev default), free users still see the normal listing, consistent with every other Pro gate in the app. When gated: the items query is skipped, the create button is hidden, and the header subtitle reads "Pro feature" instead of the item count. No new server actions/utilities/schema changes, so no new unit tests (purely presentational, reusing already-tested logic) — coding-standards test scope unaffected. Verified in-browser (Playwright) with the demo user: enforcement off → normal (empty) listing on both routes, zero console errors; enforcement temporarily flipped to `true` → both routes replaced by the upgrade panel with a working `/settings#billing` link, sidebar create buttons/count hidden; demo user temporarily flipped to `isPro=true` in the dev DB → normal listing restored with no gate; both the DB flag and `BILLING_ENFORCED` reverted to their original values afterward. Lint + build + 257 tests pass. Branched from `main` as `feature/gate-file-image-listings` (also carries the still-uncommitted seed.ts trim-to-3-collections change from the prior task, per user direction, to be split into its own commit before merge).
- **2026-08-17** — System item types seeded by migration completed. New `prisma/migrations/20260817121725_seed_system_item_types/migration.sql` inserts the 7 system `ItemType` rows, so a freshly migrated database has them without anyone running `npm run db:seed`. **Root cause:** item types are a fixed *global* list, not per-user — `getSidebarItemTypes()` (`src/lib/db/sidebar.ts`) reads `where: { isSystem: true }` with no `userId` filter and system rows carry `userId: null`. The `20260427124747_init` migration creates the table but inserts nothing, so the 7 rows existed **only** in `prisma/seed.ts` — which also plants the demo user (password `12345678`) and demo collections, making it unsafe to run against production. Any environment that had `migrate deploy` but never `db:seed` therefore had zero types and showed an empty Type selector in the create dialog to every user (`defaultType` falls back to `""`, which also hides the type-specific Content/Language/URL fields). **`ON CONFLICT` would have been a bug, not a shortcut:** the unique index is `ItemType_userId_name_key` on `("userId", "name")` and `userId` is `NULL` for system types; Postgres treats NULLs as distinct in a unique index, so that constraint never fires for system rows and `ON CONFLICT (userId, name)` would have inserted a duplicate set on **every** deploy. The migration uses `INSERT ... SELECT ... FROM (VALUES ...) WHERE NOT EXISTS (isSystem = true AND name = ...)`, mirroring the `findFirst({ where: { isSystem: true, name } })` check `prisma/seed.ts` already uses. Ids are supplied explicitly via `gen_random_uuid()::text` (built into PG13+, no extension) because `ItemType.id` has no DB-level default — Prisma generates the cuid client-side; ids are opaque throughout the app (routes key off the type *name*, nothing validates the cuid shape), verified by creating an item against a migration-created UUID row. `prisma/seed.ts` left untouched: its `upsertSystemTypes()` does `findFirst` → `update`, so it finds the migration's rows and refreshes icon/color rather than duplicating them. **Verification:** applied to the Neon **development** branch (`br-plain-smoke-ald1szfh`, endpoint `ep-dawn-pine-al63gf92` confirmed before running) where all 7 rows already existed — no-op, still one copy each with the original cuids intact. Idempotence then proven on a disposable fork (`br-weathered-cloud-al174dlj`): emptied the table → all 7 inserted; re-ran → no duplicates; deleted just `note` and re-ran → back to 7 rows / 7 distinct names with only the missing one restored. Reproduced the reported symptom end-to-end by pointing the app at that branch with zero types (Type field rendered its label with **zero** buttons and no Content field), then ran `prisma migrate deploy` — after clearing the migration record on that fork so the file genuinely ran rather than being skipped as already-applied — and the same dialog showed all 7 types with `snippet` preselected; created a snippet successfully; zero console errors. **Production (`br-spring-field-al118diy`) NOT yet migrated** — still needs `prisma migrate deploy`, and is the environment most likely to have been missing the rows. **Process lesson:** verification temporarily repointed `.env.local` at the test branch (14:28:50–14:33:29), and Next dev watches `.env` files — the user's running dev server hot-reloaded onto the emptied test DB mid-session and showed the very bug being investigated; a `pkill -f "next dev"` also killed their server. Use a one-off env override on an isolated port/process instead of editing the shared `.env.local`, and scope `pkill` to your own PID. **Follow-up identified, deferred to its own branch:** the user's *original* empty-Type-selector report on dev was almost certainly **not** missing rows (dev had all 7 the whole time) but a stale session — `AuthenticatedShell` passes `itemTypes: []` when `dbUser` is null, and `src/auth.ts`'s `jwt` callback has no `if (!dbUser) return null` guard, so a JWT whose user row no longer exists (purged account, DB reset, session minted against a different branch) stays valid indefinitely and renders a signed-in-but-empty dashboard with no user area. No new unit tests — a data migration, nothing under `src/actions/` or `src/lib/` touched, per the coding-standards test scope. Lint + build + 257 tests pass. Branched from `main` as `fix/system-item-types-migration` (clean single-fix merge).
- **2026-08-17** — Invalidate sessions whose user no longer exists completed. Fixes the state where `/` offered "Go to Dashboard" but the dashboard rendered with no user area, no items and no collections: a valid JWT whose `User` row is absent (purged account via `npm run db:purge`, reset database, or a token minted against a different Neon branch). Three layers each tolerated it — `src/proxy.ts` checks only `!!req.auth`; `src/auth.ts`'s `jwt` callback read `dbUser` as null and carried on (`dbUser?.isPro ?? false`), renewing the session indefinitely; `AuthenticatedShell` + `dashboard/page.tsx` fall back to empty defaults when `dbUser` is null, which is what rendered the blank shell (and, notably, passes `itemTypes: []` — so this same state produces an **empty Type selector in the create dialog**, which is almost certainly what was actually behind the earlier "no item types" report on dev, since dev held all 7 rows throughout). **Two changes, because the guard alone is not enough** — this was the key finding, and it came from in-browser testing rather than reasoning: `if (!dbUser) return null` in the `jwt` callback (`src/auth.ts`) fixes everything that reads the *session* — the homepage CTA, plus `/settings` and `/profile`, whose existing page-level guards then fire with `callbackUrl` intact — but it does **not** redirect `/dashboard`, because `src/proxy.ts` builds its `auth` from the edge-safe `auth.config.ts`, which has no `jwt` callback and no DB access; it can only verify that the token is signed and unexpired, so a ghost session still passes the proxy and reaches a server component that now sees a null session and renders the same empty shell. The redirect therefore has to live at the first point that can actually tell: `if (!dbUser) redirect("/sign-in")` in `AuthenticatedShell`, immediately after its `findUnique`, which covers every shell-wrapped route (`/dashboard`, `/items`, `/collections`, `/favorites`). The initial claim that the two-line `jwt` guard alone would produce a clean bounce to sign-in was wrong and was corrected after the first verification run still showed the blank shell. Also dropped the now-dead optional chaining (`dbUser?.isPro ?? false` → `dbUser.isPro`, `dbUser?.passwordChangedAt` → `dbUser.passwordChangedAt`) and the `dbUser ? … : null` fallbacks in the shell, since after the guards those branches are unreachable and implied a state that can no longer occur. A DB connection failure **throws** rather than returning null, so a transient outage does not sign anyone out. **Verification** used a git worktree with its own `.env.local` copy, its own cloned `node_modules` and its own port (:3010) against a disposable Neon fork — deliberately *not* editing the shared `.env.local` or broadly `pkill`ing, after the previous task's process lesson: signed in as demo, confirmed a healthy dashboard, then deleted the user row to create a genuine ghost session. Before the shell redirect, `/dashboard` reproduced the reported blank shell exactly; after, `/dashboard`, `/collections`, `/items/snippets` and `/favorites` all redirect to `/sign-in` with no loop, `/settings` and `/profile` redirect with `callbackUrl` preserved, the homepage shows signed-out CTAs, and the happy path on the real dev DB still renders the dashboard with the user area (no false redirect). Zero console errors. Gotcha worth noting: a git worktree is a separate checkout, so edits made in the main repo do not appear there — the first re-test failed until the changed files were copied across; and a symlinked `node_modules` breaks Turbopack ("points out of the filesystem root"), so an APFS clone copy (`cp -Rc`, ~10s for 961M) was used instead. No new unit tests — nothing under `src/actions/` or `src/lib/` is touched and no test exists for `src/auth.ts`, per the coding-standards test scope. Lint + build + 257 tests pass. Branched from `main` as `fix/invalidate-session-for-missing-user` (clean single-fix merge).
- **2026-08-18** — Plan tier badges made distinct + dedicated `/upgrade` page completed. **Part 1 — badges.** The tier indicators were muted grey chips reading as disabled text; one shared source of truth now drives them all: new `src/components/billing/plan-badge.tsx` exporting `ProBadge` (gold chip, optional `Sparkles`) and `PlanBadge` (gold `PRO` for Pro, cool sky `FREE` otherwise), sizes `sm`/`md`. **Gold was not a new decision** — the marketing homepage already brands Pro with `#f59e0b` (`⚡ Pro Feature`); sky for Free was chosen to be clearly non-gold without reading as a feature accent. Applied at ten surfaces: sidebar user row (was `bg-muted` + `text-muted-foreground`) and Files/Images item types (was a `secondary` Badge); sidebar upgrade CTA re-tinted amber (was plain `bg-accent`); `/settings` "Plan & billing" heading (new) and the `BillingSection` upgrade box; `/profile` identity card, which **showed no tier at all** (so `getProfileData` gained `isPro` in its `select`, +2 tests); the create dialog's `TypeSelector` tiles, where Pro-only types were **visually identical** to free ones (new); the `/items/files` + `/items/images` header (muted "Pro feature" subtitle → chip + "Available on DevStash Pro"); the `UpgradeRequired` panel (grey lock → amber lock + amber dashed border); and the dashboard "AI credits" stat via a new optional `StatsCard.badge` prop. **Contrast measured, not assumed** (canvas round-trip, since `getComputedStyle` returns `lab()` for these oklch-derived tokens, and my first attempt parsed lab components as RGB and produced garbage ratios): **10.51:1** Pro / **10.49:1** Free in dark, **6.32:1** in light, sidebar CTA label 13.6:1 — against ~4.4:1 for the old muted chips. Light-mode labels are `-800` not `-700` because amber-700 measured **4.48:1** on the tinted chip, marginally under AA. The app is pinned to `dark` in the root layout, so light mode was checked by removing the class at runtime; the pairing is future-proofing for a toggle that doesn't exist yet. **Two layout gotchas found in-browser:** the `TypeSelector` chip at `-top-2` floated into the 8px grid gap and read as belonging to the tile *above*, so it moved inside the tile corner (`right-1 top-1`); and `UpgradeRequired`'s heading rendered a **double space** after "Files" because bare text between flex items becomes an anonymous flex item whose literal spaces stack on top of the `gap` — each run is now its own `<span>`. Also dropped the chip from `BillingSection`'s Pro box: the section heading already carries one a few pixels away, and two PRO badges that close together just read as noise. **Part 2 — `/upgrade`.** New `src/app/upgrade/page.tsx` (standalone shell like `/settings`/`/profile`, added to `src/proxy.ts` protected routes) showing a Free-vs-Pro comparison modelled on the homepage pricing area, plus `src/components/billing/upgrade-plans.tsx` (client) owning a monthly/yearly segmented control and calling the **unchanged** `createCheckoutSession(interval)`. Feature lists are **reused** from `@/components/home/data` (`FREE_PLAN`/`PRO_PLAN`) so the marketing and in-app surfaces can't advertise different things — only the data is shared, not the homepage's `.home` scoped palette. AUD price copy was extracted from `billing-section.tsx` into new `src/lib/plan-pricing.ts` so both upgrade surfaces read one source (+4 tests, including a guard that every amount keeps its `AUD` label — Checkout charges AUD and a bare `$` misreads as USD). Header CTA in `dashboard-shell.tsx` for **Free users only**, per the user's brief: `ghost` variant on `text-muted-foreground`, deliberately the lightest control in the header, label at `md`+ and icon-only 36×36 below (matching its icon-button siblings) with `aria-label="Upgrade to Pro"`. The sidebar CTA and the `UpgradeRequired` button were **repointed** from `/settings#billing` to `/upgrade`, making it the single upgrade funnel and leaving `/settings` for billing *management* — the settings box keeps its own direct checkout (no regression), which is a **remaining duplication**: two divergent upgrade UIs, and it could later be reduced to a link. `/upgrade` redirects to `/settings#billing` for a Pro user **and** for a lapsed subscription (`stripeSubscriptionId` present but not Pro) — sending the latter to Checkout would create a *second* subscription, the Phase 1 finding `BillingSection` already guards. **Verification method matters here:** all three dev users are `isPro`, so the Free surfaces were exercised with a **temporary local patch** (forcing `isPro: false` / `requiresUpgrade: true`) rather than mutating the user's dev DB or editing the shared `.env.local` — per the earlier process lesson that Next hot-reloads `.env` files into the user's running dev server — then reverted with no `TEMP-VERIFY` markers left. Confirmed: Pro chips on all Pro surfaces; Free state showing the sky chip, amber CTAs, header ghost button, `/items/files` gate panel, and `PRO` chips on the File/Image tiles; the Pro→`/settings#billing` redirect **with real DB state**; the toggle rewriting the Pro price ($8 AUD/month ↔ $72 AUD/year + "Save 25% vs monthly" + "Works out to $6 AUD per month"); mobile stacking at 375px with zero overflow. The checkout button is wired end-to-end — clicking it returned **"You're already on Pro."**, which also proves the server guard holds independently of the UI — but the **Stripe redirect itself was not re-exercised**, since a genuine test-mode checkout needs a user's `isPro` flipped to `false` in the DB; that action is unchanged from Phase 2. Zero console errors at 375/1440px. **A `/cleanup` pass ran in the same session and its fixes are in the second commit:** the History list had three entries **prepended** above an otherwise oldest-to-newest list (contradicting the file's own "Earliest to latest" directive) — reordered, all 56 entries preserved, the `2026-08-05` gate-Pro entry placed after both Stripe entries per commit order (`e8ac0df`); deleted `src/components/ui/badge.tsx`, orphaned by *this* feature (its only consumer was the sidebar PRO chip); **removed the "Recently used" sidebar link**, since `/dashboard/recent` has never existed and 404'd for every user — note this drops the entry point for a still-spec'd feature, to be re-added with the page; documented the four `User` columns added since the `project-overview.md` draft (`stripeSubscriptionStatus`, `stripePriceId`, `stripeCurrentPeriodEnd`, `editorPreferences`); labelled both pricing tables **AUD** with the reason (currency can't be changed on an existing Stripe price, and adaptive pricing may show a converted local amount) — they previously read as USD; and documented the 7 missing npm scripts in `CLAUDE.md` plus the fact that system `ItemType`s come from a migration rather than `db:seed`. Clean findings: no `console.log`, no unused imports (`no-unused-vars` is active at warn and eslint exits 0), no TODO/FIXME, no `@ts-ignore`, and `.env.local`/`.env.production`/`.env.example` carry identical variable sets. **Kept by user direction:** `src/lib/mock-data.ts` (169 dead lines, nothing imports it) and `prototypes/homepage/`. **Still open:** `STRIPE_PUBLISHABLE_KEY` is declared in all three env files and read nowhere (hosted Checkout needs no Stripe.js); `PRODUCTION_URL` exists in `.env.production` and nothing in the codebase reads it; `getPlanUsage` adds two `count` queries per create; whether the dashboard should warn as a Free user nears a limit; Stripe event **ordering** (a stale `updated` after `deleted` would resurrect a subscription); and the un-exercised checkout redirect above. Lint + build + **263 tests** pass. Committed as `feat: add distinct plan tier badges and a dedicated upgrade page` (`553d704`) and `chore: fix doc drift and remove dead code found by cleanup` (`078a272`), merged as `a7ae790`. Branched from `main` as `feature/plan-tier-badges-and-upgrade-page` (renamed mid-task from `feature/distinct-plan-badges` once the upgrade page joined the scope); the two efforts share three files, so they could not be split into build-clean separate commits.
- **2026-08-18** — Duplicate upgrade UI removed from `/settings` completed. Closes the duplication logged in the previous entry: once `/upgrade` shipped, a Free user had **two divergent checkout surfaces** — the settings billing box carried its own monthly/yearly segmented toggle and its own direct-to-Stripe button, while `/upgrade` showed the full plan comparison — so they could drift apart and neither was obviously canonical. The Free branch of `BillingSection` now keeps its usage meters and replaces the toggle + checkout button with a single **"View plans"** link to `/upgrade`; **`/upgrade` is the only checkout surface and `/settings` is billing management**. The price stays visible as one line ("$8 AUD per month or $72 AUD per year — save 25% vs monthly.") read from `PLAN_PRICING`, so neither surface can quote a different number and the shared module keeps both consumers. Label is **"View plans"** rather than "Upgrade to Pro": it navigates to a comparison page rather than straight to Stripe, and the box heading already says "Upgrade to `PRO`", so the intent is carried without promising a checkout that isn't one click away. Dropped the `interval` state and, with it, the `createCheckoutSession`, `BillingInterval` and `useState` imports — `useTransition`/`go()` stay, because the Pro and lapsed branches both still open the Stripe portal; grep confirms `/upgrade` is now `createCheckoutSession`'s only caller. Net −48/+41 lines in `billing-section.tsx`, and the Pro and lapsed branches are untouched. **Deliberately left alone:** checkout's `cancel_url` still returns to `/settings?checkout=cancelled` rather than back to `/upgrade`, because `CheckoutToast` lives on `/settings` — a slightly odd bounce, but moving it would mean duplicating the toast handler. `success_url` landing on `/settings` is now *more* coherent than before: you leave from `/upgrade` and arrive on the management page where the Pro state renders. **No new unit tests** — presentational refactor of one client component, nothing under `src/actions/` or `src/lib/` touched, per the coding-standards test scope; the existing `createCheckoutSession` tests still cover the unchanged action. **Verified in-browser** on a throwaway dev server on **:3010** (the user's own server was down and starting one on :3000 would have collided), again using a temporary local patch rather than mutating the dev DB, since all three dev users are `isPro`: Free showed the meters, no toggle, the single price line and "View plans" navigating to `/upgrade`; Pro was unchanged (gold status box, renewal line, Manage subscription); lapsed (`past_due`) was unchanged (red warning panel, Update payment details → portal). Patch artifact noted so it isn't mistaken for a bug later: while faking Free/lapsed, the section heading still read `PRO` / "You're on DevStash Pro.", because the patch overrode only `BillingSection`'s `status` prop and not the page's own `billing.isPro` reads — real code drives both from the same value. Temp patch reverted with no `TEMP-VERIFY` markers left; zero console errors. Lint + build + **263 tests** pass. Committed as `fix: remove the duplicate upgrade UI from the settings page` (`6866e2f`), merged as `680a4b7`. Branched from `main` as `fix/settings-upgrade-duplication` (clean single-fix merge).
- **2026-08-19** — Language dropdown above the content editor completed. The free-text Language `Input` on code items is now a **dropdown**, and it moved **above** the Content editor in both the new item modal and the drawer edit form, so the choice reads as a setting for the editor beneath it. New `src/lib/code-language.ts` exports — `LanguageOption`, `LANGUAGE_OPTIONS` (26 languages + a "None" entry) and `languageOptions(current)`. **Option values are canonical Monaco ids** (e.g. `shell`, not `bash`) so they highlight without passing through `LANGUAGE_ALIASES`; a unit test enforces that by asserting every option value round-trips through `monacoLanguage` unchanged. `languageOptions(current)` **appends an unlisted current value as its own option** — items created before the dropdown existed can hold any free text, and without that a `<select>` whose value matches no option falls back to the first one, so opening the edit form and saving would have silently rewritten the language to "None". A read-only check of the dev branch found no such values (`typescript` ×3, `dockerfile`), so that path is covered by unit test rather than in-browser. New shared `src/components/items/language-select.tsx` (`LanguageSelect`) wraps a native `<select>` in the existing `FormField`, styled to match `Input` and the `/settings` selects, and is used by both `item-create-dialog.tsx` (`create-language`, disabled while creating) and `item-edit-form.tsx` (`edit-language`, disabled while saving) — both had their old `Input` block deleted and the new field placed before `ContentField`. **No editor changes were needed**: `language` state already flowed into `ContentField` → `CodeEditor` → `monacoLanguage()`, so live highlighting came for free; this feature is the dropdown plus the reordering. The gate is unchanged (`isCodeType`, so snippet + command only). **Follow-up fix in the same session, and the one non-obvious finding:** the user asked for more padding so the dropdown arrow wasn't flush against the right edge, but **Chrome's native select arrow ignores `padding-right`** — measured at a fixed **4.5px** from the border at `padding-right` 12/16/20/24px, and confirmed to be the arrow (not the border) because it disappeared under `appearance: none` while the same scan then found nothing bright to the right. So `pr-4` alone was inert; the fix is `appearance-none` on the select plus a `pointer-events-none` lucide `ChevronDown` absolutely positioned at `right-3`, which also makes the control render consistently rather than deferring to each platform's native arrow. Re-measured at **15.5px** inset, and the text box ends 8px before the icon starts, so no label can collide with it. Scoped to this component — the `/settings` editor-preference selects still use their native arrow. Colocated unit tests +4 (`languageOptions`: base-list identity for empty/nullish/whitespace and for listed values, unlisted-append without mutating `LANGUAGE_OPTIONS`, and the canonical-id guard). No component tests, per the coding-standards scope. Verified in-browser (Playwright, demo user, dev server on :3000): with "None" the content was a single `mtk1` plaintext token; selecting TypeScript retokenized live (`const`/`string` `#569cd6`, string `#ce9178`, comment `#608b4e`) and the editor header badge changed to `typescript`; typing after selecting kept highlighting; switching to Python retokenized again; created a snippet, reopened it in the drawer → Edit showed **TypeScript preselected**, switched to Shell/Bash, saved, and the drawer view badge read `shell` (test item deleted afterwards, snippets back to 4); `/items/commands` behaved identically. Only console entry was the known-benign Monaco dispose `Canceled` rejection on unmount. Lint + build + **267 tests** pass. Committed as `feat: select item language from a dropdown above the content editor` (`4f9cc2c`), merged as `6924ecf`. Branched from `main` as `feature/language-dropdown` (clean single-feature merge). Process note: `.env.example` plus `context/research/ai-integration-research.md` and `docs/ai-integration-plan.md` were already dirty/untracked when the session began, so the commit staged only the five feature files and left those alone.
